# Chapter 5: Neural Networks - Advanced MCQ Assessment

**1. In the context of biological inspiration for Artificial Neural Networks, which physical component of a biological neuron is mathematically abstracted as the "weight" matrix?**
* A) The Soma
* B) The Synapse
* C) The Dendrite
* D) The Axon

**2. The McCulloch-Pitts (MCP) neuron model fundamentally differs from modern artificial neurons due to which strict limitation?**
* A) It relies on continuous floating-point weights and biases.
* B) It utilizes a linear activation function exclusively.
* C) Its inputs and outputs are strictly binary, and it lacks a learning mechanism for its weights.
* D) It incorporates backpropagation to adjust threshold values dynamically.

**3. Geometrically, what effect does the "bias" parameter have on the decision boundary of a single-layer perceptron in a 2D feature space?**
* A) It alters the angle (slope) of the decision boundary relative to the origin.
* B) It translates the decision boundary parallel to itself, moving it away from or towards the origin.
* C) It transforms the linear decision boundary into a polynomial curve.
* D) It projects the input features into a higher-dimensional space to achieve linear separability.

**4. The 1969 publication "Perceptrons" by Minsky and Papert demonstrated that a single-layer perceptron cannot solve the XOR problem. What is the fundamental mathematical reason for this?**
* A) XOR requires continuous inputs, which the perceptron cannot process.
* B) The perceptron learning rule inherently diverges when faced with symmetric data.
* C) The XOR logic gate generates a non-linearly separable dataset, meaning no single straight line can perfectly divide the classes.
* D) The threshold logic unit (TLU) evaluates to zero for all XOR input combinations.

**5. Assuming a threshold logic unit (TLU) uses a step function that outputs 1 if $\sum w_i x_i + b \ge 0$, which set of parameters correctly models a logical AND gate for binary inputs $x_1, x_2$?**
* A) $w_1 = 1, w_2 = 1, $b=-0.$5$
* B) $w_1 = 1, w_2 = 1, $b=-1.$5$
* C) $w_1 = -1, w_2 = -1, $b=1.$5$
* D) $w_1 = 0.5, w_2 = 0.5, $b=0.$5$

**6. If a Perceptron updates its weights using the rule $\Delta w_i = \eta (y - \hat{y}) x_i$, under what condition will the weight $w_i$ remain completely unchanged?**
* A) Only when the learning rate $\eta$ is set to 1.
* B) When the predicted output $\hat{y}$ correctly matches the true target $y$, or when the input feature $x_i$ is 0.
* C) When the network reaches the maximum number of defined epochs.
* D) Only when the bias term is updated instead of the weights.

**7. Why is the standard Perceptron Learning Rule incapable of being used with a Multi-Layer Perceptron (MLP)?**
* A) The Perceptron Rule requires a hard step activation function, which is non-differentiable and thus prevents the calculation of error gradients for hidden layers.
* B) MLPs do not utilize weights and biases in their hidden layers.
* C) The Perceptron Rule automatically prunes hidden layers to optimize memory.
* D) MLPs only accept continuous numerical inputs, while the Perceptron Rule requires binary text data.

**8. Which theorem formally guarantees that a Feedforward Neural Network with at least one hidden layer containing a finite number of non-linear neurons can approximate any continuous function?**
* A) The No Free Lunch Theorem
* B) The Universal Approximation Theorem
* C) Bayes' Theorem
* D) The Markov Decision Process

**9. What is the exact mathematical derivative of the standard Logistic Sigmoid function, $f(x)$, with respect to $x$?**
* A) $f(x) \cdot (1 - f(x))$
* B) $1 - f(x)^2$
* C) $f(x) + (1 - f(x))$
* D) $e^{-x} / (1 + e^{-x})^2$

**10. What specific problem arises when applying the Sigmoid activation function across many sequential hidden layers in a deep neural network?**
* A) The Exploding Gradient Problem
* B) The Dying ReLU Problem
* C) The Vanishing Gradient Problem
* D) The Curse of Dimensionality

**11. How does the Hyperbolic Tangent (Tanh) activation function offer an optimization advantage over the standard Sigmoid function?**
* A) It is computationally faster to calculate because it avoids exponential math.
* B) It guarantees immunity against vanishing gradients.
* C) Its output is zero-centered (ranging from -1 to 1), which helps center the data for the next layer and prevents weight updates from skewing entirely in one direction.
* D) It outputs only strictly positive integers, aligning perfectly with categorical cross-entropy.

**12. The ReLU (Rectified Linear Unit) function is mathematically defined as $f(x) = \max(0, x)$. What is its derivative when $x < 0$?**
* A) 1
* B) 0
* C) -1
* D) Undefined

**13. What is the "Dying ReLU" phenomenon in neural network training?**
* A) When ReLU neurons output exponentially large values, causing the loss function to overflow.
* B) When a large negative bias update causes the input to the ReLU to constantly be negative, resulting in a perpetual output of 0 and a gradient of 0, effectively permanently disabling the neuron.
* C) When the learning rate decays to zero before the network converges.
* D) When the network drops the ReLU activation function in favor of a linear one due to overfitting.

**14. To solve a multi-class classification problem with 5 distinct, mutually exclusive classes, what is the most appropriate activation function for the output layer?**
* A) Linear
* B) Sigmoid
* C) ReLU
* D) Softmax

**15. By what specific mechanism does the Softmax function transform a vector of raw logit scores into a valid probability distribution?**
* A) By dividing each logit by the total number of classes.
* B) By applying the absolute value to each logit and scaling them to a maximum of 1.
* C) By exponentiating each logit and dividing by the sum of all exponentiated logits.
* D) By subtracting the minimum logit from all values and dividing by the maximum logit.

**16. Why is Mean Squared Error (MSE) generally considered a poor choice for the loss function in a classification network using Sigmoid/Softmax outputs?**
* A) MSE combined with Sigmoid creates a highly non-convex loss landscape with many local minima and extremely flat regions that severely stall gradient descent.
* B) MSE cannot output positive numbers.
* C) MSE requires the target variables to be continuous strings.
* D) MSE forces the network to execute backpropagation in the forward direction.

**17. What does Categorical Cross-Entropy loss fundamentally measure during network training?**
* A) The Euclidean distance between the weight vectors.
* B) The divergence or difference between the true target probability distribution (usually one-hot encoded) and the model's predicted probability distribution.
* C) The sum of the squared weights in the final hidden layer.
* D) The ratio of True Positives to False Positives.

**18. During the Feedforward phase of a neural network layer defined by $Z = WX + b$, if $X$ is a feature vector of shape $(3, 1)$ and the layer has 4 neurons, what must be the shape of the weight matrix $W$?**
* A) $(3, 4)$
* B) $(4, 3)$
* C) $(1, 4)$
* D) $(4, 1)$

**19. Backpropagation utilizes the Chain Rule of calculus. If we have an error $E$, a network output $y$, and a hidden weight $w$, how is the gradient $\frac{\partial E}{\partial w}$ conceptually constructed?**
* A) $\frac{\partial E}{\partial y} + \frac{\partial y}{\partial w}$
* B) $\frac{\partial E}{\partial w} \cdot \frac{\partial y}{\partial E}$
* C) $\frac{\partial E}{\partial y} \cdot \frac{\partial y}{\partial z} \cdot \frac{\partial z}{\partial w}$ (where $z$ is the net input)
* D) $\int E \, dy \cdot \int y \, dw$

**20. What is the fundamental difference between an Epoch and an Iteration in neural network training?**
* A) An epoch updates the bias; an iteration updates the weights.
* B) An epoch is one forward/backward pass of the *entire* dataset; an iteration is one forward/backward pass of a single *batch*, resulting in one weight update.
* C) An iteration happens during forward propagation; an epoch happens during backpropagation.
* D) There is no mathematical or functional difference between the two terms.

**21. In standard Batch Gradient Descent, how often are the network's weights updated?**
* A) After every single training example is processed.
* B) After every randomly selected mini-batch is processed.
* C) Once per epoch, after the entire training dataset has been processed and the average gradient is computed.
* D) Only after the validation loss stops decreasing.

**22. What is the primary advantage of Stochastic Gradient Descent (SGD) over Batch Gradient Descent for massive datasets?**
* A) SGD guarantees convergence to the absolute global minimum without oscillation.
* B) SGD performs weight updates much more frequently (after every individual sample), allowing it to make faster progress and escape local minima due to its inherent noise, while requiring vastly less memory.
* C) SGD requires no hyperparameter tuning for the learning rate.
* D) SGD mathematically eliminates the need for backpropagation.

**23. How does the "Momentum" optimization technique accelerate gradient descent?**
* A) It randomly drops weights to reduce computational drag.
* B) It accumulates a moving average of past gradients to smooth out oscillations and accelerate movement in consistent directions.
* C) It exponentially increases the learning rate at every epoch.
* D) It dynamically changes the activation function based on the speed of convergence.

**24. Which popular adaptive learning rate optimization algorithm combines the concepts of Momentum (first moment) and RMSprop (second moment) to individually scale the learning rate of each parameter?**
* A) Mini-batch SGD
* B) AdaGrad
* C) L-BFGS
* D) Adam

**25. If a deep neural network has 5 hidden layers and utilizes the Sigmoid activation function, what happens to the error gradient as it propagates backward to the first hidden layer?**
* A) It becomes exponentially larger, causing integer overflow.
* B) It remains exactly constant due to the conservation of energy.
* C) It is multiplied by a maximum value of 0.25 at each layer, decaying exponentially towards zero and preventing the early layers from learning.
* D) It forces the biases of the first layer to become strictly negative.

**26. Why is a purely linear neural network (where every layer uses a linear activation function $f(x)=x$) effectively useless for deep learning?**
* A) Linear functions are computationally impossible to differentiate.
* B) The composition of multiple linear functions is mathematically equivalent to a single linear function, meaning the network can only learn linearly separable patterns regardless of depth.
* C) Linear networks require categorical string data to function.
* D) Linear functions automatically trigger the exploding gradient problem on the first epoch.

**27. What is the standard structural configuration required to solve the XOR logic gate using a Multi-Layer Perceptron with step activation functions?**
* A) 1 input layer (2 nodes), 0 hidden layers, 1 output layer.
* B) 1 input layer (2 nodes), 1 hidden layer (2 nodes), 1 output layer (1 node).
* C) 1 input layer (2 nodes), 2 hidden layers (4 nodes each), 1 output layer (2 nodes).
* D) XOR cannot be solved by any Feedforward Neural Network architecture.

**28. What does "Overfitting" practically mean when evaluating the performance curves of a neural network over time?**
* A) Both training loss and validation loss remain exceptionally high.
* B) Both training loss and validation loss decrease to zero simultaneously.
* C) Training loss continues to decrease, but validation loss begins to increase, indicating the model is memorizing training noise rather than generalizing.
* D) The network begins to randomly output NaN values.

**29. The regularization technique known as "Dropout" achieves its effect by: **
* A) Permanently deleting the lowest-weighted neurons at the end of training.
* B) Randomly setting the output of a fraction of hidden neurons to zero during each forward pass of the training phase, forcing the network to learn redundant, robust representations.
* C) Dropping outliers from the training dataset before the first epoch.
* D) Stopping the training early if the validation loss drops too quickly.

**30. How does L2 Regularization (Weight Decay) modify the standard loss function $J(W)$?**
* A) It subtracts the sum of the absolute values of the weights from the loss.
* B) It adds a penalty term proportional to the sum of the squared weights: $J(W) + \lambda \sum w^2$.
* C) It multiplies the loss by the learning rate.
* D) It divides the loss by the number of hidden layers.

**31. What is the primary purpose of creating a "Validation Set" separate from the Training and Test sets?**
* A) To explicitly test the final model after all training and tuning is completed.
* B) To artificially inflate the size of the training dataset via augmentation.
* C) To provide a completely unseen dataset during the training loop for the purpose of tuning hyperparameters (like learning rate or architecture) and executing early stopping without leaking the final Test data.
* D) To replace the loss function mathematically.

**32. When initializing the weights of a neural network, why is initializing all weights to exactly zero a critical error?**
* A) It causes the learning rate to default to zero.
* B) It causes all neurons in a given layer to compute the exact same output and receive the exact same gradient update, breaking the asymmetry needed to learn distinct features.
* C) It causes the cross-entropy loss function to return undefined values.
* D) It forces the network to skip the forward propagation step.

**33. What is the purpose of "Xavier/Glorot Initialization" or "He Initialization"?**
* A) To set all biases to exactly 1.0.
* B) To randomly initialize weights from a distribution scaled by the number of inputs/outputs to the layer, preserving the variance of activations and gradients to prevent them from vanishing or exploding early in training.
* C) To automatically determine the optimal number of hidden layers.
* D) To ensure the network only uses ReLU activation functions.

**34. In a binary classification task, if the output neuron uses a Sigmoid activation, what does a raw output value of 0.85 represent?**
* A) The margin of error in percentage.
* B) The gradient of the loss function.
* C) The model estimates an 85% probability that the input belongs to the positive class (class 1).
* D) The network requires 85 more epochs to converge.

**35. If you are building a regression model to predict the continuous numerical price of a car, what activation function must be used on the final output neuron?**
* A) Sigmoid
* B) Softmax
* C) Linear (Identity)
* D) Tanh

**36. During training, if your network's cost function oscillates wildly up and down with high variance rather than smoothly descending, what is the most likely cause?**
* A) The dataset is too perfectly normalized.
* B) The learning rate $\eta$ is set too high.
* C) The batch size is exactly equal to the total dataset size.
* D) The network is using L2 Regularization.

**37. What is "Data Augmentation" in the context of training deep networks (especially for images)?**
* A) Artificially expanding the training dataset by applying random transformations (rotations, flips, zooms) to existing data to reduce overfitting and improve generalization.
* B) Duplicating the identical dataset 10 times to increase the number of epochs.
* C) Increasing the bit-depth of the data from 8-bit to 32-bit.
* D) Adding synthetic, highly correlated features to tabular data.

**38. Which of the following describes a "Fully Connected" (or Dense) Neural Network layer?**
* A) Every neuron in the layer is connected exclusively to one corresponding neuron in the next layer.
* B) The layer contains no activation functions.
* C) Every single neuron in the layer receives an input from every single neuron in the immediately preceding layer.
* D) The layer loops its outputs back into its own inputs.

**39. In a network where layer $L$ has 10 neurons and layer $L+1$ has 5 neurons, how many individual weight parameters connect these two layers? (Ignore biases)**
* A) 15
* B) 25
* C) 50
* D) 100

**40. Which architecture is defined by having connections that form a directed cycle, allowing it to maintain a "hidden state" or memory of past sequential inputs?**
* A) Feedforward Neural Network
* B) Convolutional Neural Network (CNN)
* C) Radial Basis Network
* D) Recurrent Neural Network (RNN)

**41. What is the fundamental difference between a CNN (Convolutional Neural Network) and a standard Feedforward Dense Network?**
* A) CNNs cannot perform classification.
* B) CNNs do not use backpropagation for training.
* C) CNNs use shared weights and local receptive fields (filters) to capture spatial hierarchies, drastically reducing parameters compared to connecting every input to every neuron.
* D) CNNs only accept 1-dimensional time-series data.

**42. Which metric calculates the ratio of correctly predicted positive observations to the total predicted positives in a classification matrix?**
* A) Recall
* B) Precision
* C) F1-Score
* D) Accuracy

**43. A neural network classifies 100 emails. It identifies 20 as Spam. Of those 20, 15 were actually Spam, and 5 were Not Spam. What is the Precision of the Spam class?**
* A) 15/100 (15%)
* B) 15/20 (75%)
* C) 5/20 (25%)
* D) 20/100 (20%)

**44. What does the F1-Score mathematically represent?**
* A) The arithmetic mean of Precision and Recall.
* B) The harmonic mean of Precision and Recall.
* C) The ratio of True Positives to False Negatives.
* D) The absolute difference between training loss and validation loss.

**45. If an architecture is described as having "1 hidden layer," how many layers of *computational weights* does it actually possess?**
* A) 0
* B) 1
* C) 2
* D) 3

**46. What does the `batch_size` parameter specifically control during network training?**
* A) The number of epochs before early stopping triggers.
* B) The number of neurons randomly dropped during regularization.
* C) The maximum size of the weight gradients allowed before clipping.
* D) The number of training samples propagated through the network before the optimizer computes the gradient and updates the weights.

**47. "Early Stopping" acts as a form of implicit regularization by doing what?**
* A) Setting weights to zero if they do not change after 5 epochs.
* B) Terminating the gradient descent process when the error on the validation set begins to increase, thereby preventing the model from deeply memorizing the training set.
* C) Stopping the forward pass at the hidden layers if the output is already highly confident.
* D) Eliminating the bias terms entirely.

**48. Why is standardizing or normalizing input features critical before feeding them into a Neural Network?**
* A) It allows the network to process categorical text natively.
* B) It ensures the loss landscape is more symmetric and spherical, allowing gradient descent to converge much faster and preventing features with massive scales from dominating the weight updates.
* C) It permanently forces the test set accuracy to exactly match the training set accuracy.
* D) It converts non-linear datasets into linearly separable datasets.

**49. Which of the following is true concerning the mathematical loss landscape of Deep Neural Networks?**
* A) It is always a perfectly convex bowl, ensuring global convergence.
* B) It is a highly complex, non-convex, multi-dimensional surface riddled with local minima, saddle points, and plateaus.
* C) It is completely flat until the final epoch.
* D) It only exists if the ReLU activation function is avoided.

**50. What is a "Saddle Point" in the context of gradient descent optimization?**
* A) The absolute lowest point of the cost function.
* B) A point where the gradient is strictly vertical.
* C) A point where the gradient is zero, but it represents a minimum in one cross-section and a maximum in another, often severely stalling basic gradient descent algorithms.
* D) A hyperparameter used to define the learning rate decay schedule.

**51. In the equation $a = f(W \cdot X + b)$, what does the function $f()$ symbolize?**
* A) The loss function
* B) The learning rate schedule
* C) The activation function
* D) The gradient clipping threshold

**52. What causes a single-layer perceptron to fail at learning the XOR function?**
* A) It lacks a continuous learning rate.
* B) It requires exactly 3 inputs, but XOR only provides 2.
* C) XOR's truth table plots points that cannot be cleanly separated into two classes by a single straight line.
* D) XOR is a continuous regression problem, while the perceptron is a classifier.

**53. To create a multi-class probability distribution where the sum of all node outputs is exactly 1.0, the Softmax function calculates:**
* A) The mean absolute error of each node.
* B) The exponential of each specific logit divided by the sum of the exponentials of all logits in the layer.
* C) The raw output value divided by the maximum output value.
* D) The square root of the sum of squared weights.

**54. In backpropagation, if the gradient of the loss with respect to a specific weight evaluates to exactly zero ($\frac{\partial L}{\partial w} = 0$), what occurs during the weight update phase?**
* A) The weight is halved.
* B) The weight is multiplied by the learning rate.
* C) The weight is inverted.
* D) The weight remains completely unchanged for that iteration.

**55. Which of the following accurately describes "One-Hot Encoding" for a categorical target variable with 4 classes?**
* A) Transforming the classes into the integers 1, 2, 3, and 4.
* B) Representing Class 2 as an array: `[0, 1, 0, 0]`.
* C) Converting the target into a single string containing the class name.
* D) Scaling the classes between -1 and 1.

**56. If a model predicts probabilities of `[0.1, 0.8, 0.1]` for a 3-class problem, and the true label is Class 1 (one-hot: `[0, 1, 0]`), which loss function is best utilized to calculate the error?**
* A) Mean Squared Error (MSE)
* B) Binary Cross-Entropy
* C) Categorical Cross-Entropy
* D) Huber Loss

**57. Why are hidden layers referred to as "hidden"?**
* A) Because their specific internal weights are encrypted and inaccessible to the programmer.
* B) Because their true target outputs are unknown; the network only receives explicit target data for the final output layer.
* C) Because they do not utilize activation functions.
* D) Because they are physically skipped during the forward propagation phase.

**58. What is the fundamental characteristic of "Batch Normalization"?**
* A) It normalizes the final probabilities to sum to 1.
* B) It standardizes the activations of a given layer (mean 0, variance 1) for each mini-batch, helping to mitigate the vanishing gradient problem and allowing higher learning rates.
* C) It forces the dataset to be loaded entirely into RAM.
* D) It converts categorical features into numerical vectors.

**59. If your neural network exhibits a severe degree of high variance (overfitting), which of the following actions is MOST likely to help?**
* A) Drastically increasing the number of hidden layers and neurons.
* B) Removing all dropout and regularization layers.
* C) Increasing the amount of training data or applying Data Augmentation.
* D) Changing the loss function from Cross-Entropy to MSE.

**60. The technique of "Weight Decay" is synonymous with which regularization method?**
* A) L1 Regularization (Lasso)
* B) L2 Regularization (Ridge)
* C) Dropout
* D) Early Stopping

**61. What is the primary operational difference between the L1 and L2 regularization penalties?**
* A) L1 squares the weights; L2 takes the absolute value.
* B) L1 drives many less important weights to exactly zero (feature selection), while L2 shrinks all weights proportionally toward zero without completely eliminating them.
* C) L1 operates on the biases; L2 operates on the weights.
* D) L1 is used for regression; L2 is used for classification.

**62. If you need a network to output multiple independent binary labels simultaneously (e.g., an image can be both "Cat" and "Indoors"), what is the optimal output layer configuration?**
* A) A Softmax activation across all output neurons.
* B) A single linear neuron.
* C) Multiple independent neurons, each using a Sigmoid activation function and evaluated with Binary Cross-Entropy.
* D) Multiple independent neurons using the Tanh activation function.

**63. What is "Transfer Learning" in the domain of deep neural networks?**
* A) Transferring the dataset from a hard drive to GPU memory.
* B) Reusing a highly optimized, pre-trained model (trained on a massive dataset) as the starting point for a new, related task, rather than training a network from scratch.
* C) Changing the activation function from Sigmoid to ReLU during training.
* D) Translating Python code into a C++ execution environment.

**64. During gradient descent, what mathematical entity defines the direction of the steepest ascent of the loss function?**
* A) The Bias
* B) The Hessian Matrix
* C) The Gradient vector
* D) The Activation Function

**65. To move towards the minimum of the loss function, the weight update rule subtracts the gradient multiplied by the learning rate. Why is it subtracted rather than added?**
* A) Because weights must always be negative.
* B) Because the gradient points in the direction of the steepest *ascent* (increase) of the function, so subtracting it forces the parameters to move in the direction of the steepest *descent* (decrease).
* C) Because learning rates are intrinsically negative values.
* D) Because matrix addition is computationally heavier than subtraction.

**66. What is the "Architecture" of a Neural Network specifically defining?**
* A) The exact final values of the weights and biases after training.
* B) The hardware specifications (CPU/GPU) the model is running on.
* C) The structural arrangement of the network, including the number of layers, the number of neurons per layer, and how they are interconnected.
* D) The specific dataset used to train the model.

**67. In a classification report, what is the definition of "Recall" (also known as Sensitivity or True Positive Rate)?**
* A) $TP / (TP + FP)$
* B) $TP / (TP + FN)$
* C) $TN / (TN + FP)$
* D) $FP / (FP + TN)$

**68. If a medical diagnostic neural network must avoid missing any actual cancer patients (even at the cost of some false alarms), which metric should be heavily prioritized and maximized?**
* A) Precision
* B) Specificity
* C) Recall
* D) Exact Accuracy

**69. Which algorithm was introduced in 1986 by Rumelhart, Hinton, and Williams, finally providing a robust method for training multi-layer networks and revitalizing the field of AI?**
* A) The Normal Equation
* B) Support Vector Machines
* C) The Backpropagation Learning Algorithm
* D) K-Means Clustering

**70. What happens during the "Forward Pass" (Forward Propagation) of a Neural Network?**
* A) The error is calculated and derivatives are pushed through the network.
* B) The weights are randomly initialized.
* C) The input data is multiplied by the weights, biases are added, and activation functions are applied layer by layer until an output prediction is generated.
* D) The validation set is evaluated.

**71. How does a "Leaky ReLU" mathematically differ from a standard "ReLU"?**
* A) ReLU outputs $-1$ for negative inputs; Leaky ReLU outputs $0$.
* B) ReLU outputs $0$ for negative inputs; Leaky ReLU outputs a small, scaled version of the input (e.g., $0.01x$) for negative inputs.
* C) Leaky ReLU applies a logarithmic curve to positive inputs.
* D) There is no mathematical difference; they are just optimized differently on GPUs.

**72. Why is calculating the derivative of the Step Function problematic for gradient-based optimization?**
* A) The derivative is undefined at 0, and exactly 0 everywhere else, providing no useful gradient information for weight updates.
* B) The derivative evaluates to infinity at all points.
* C) The derivative is a complex number.
* D) The step function requires non-linear algebra to compute.

**73. In the context of neural network architectures, what does "MLP" stand for?**
* A) Machine Learning Pipeline
* B) Multi-Layer Perceptron
* C) Maximum Likelihood Parameter
* D) Matrix Linear Processing

**74. What is the fundamental difference between a "Parameter" and a "Hyperparameter"?**
* A) Parameters (like weights) are learned automatically by the algorithm during training; Hyperparameters (like learning rate) are manually set by the designer prior to training.
* B) Hyperparameters are updated via backpropagation, while parameters remain static.
* C) Parameters apply only to input layers, whereas hyperparameters apply only to output layers.
* D) There is no functional difference.

**75. If the training loss curve of your neural network looks like a smooth 'U' shape (decreasing at first, then sharply increasing), what is the definitive cause?**
* A) The network has successfully converged.
* B) The learning rate is much too high, causing the algorithm to massively overshoot the optimal minimum.
* C) The batch size is too small.
* D) The model requires an extra hidden layer.

**76. What is the "Curse of Dimensionality" concerning neural network inputs?**
* A) Using 3D input matrices causes standard architectures to crash.
* B) As the number of input features increases, the volume of the data space grows exponentially, requiring exponentially more data to effectively train the network without severe overfitting.
* C) Dimensions beyond 4 cannot be represented by matrix multiplication.
* D) Adding more features permanently caps the accuracy at 50%.

**77. The mathematical formula $E = \frac{1}{n} \sum (\hat{y} - y)^2$ calculates what metric?**
* A) Cross-Entropy Loss
* B) Mean Absolute Error (MAE)
* C) Mean Squared Error (MSE)
* D) R-squared

**78. Which of the following is an example of an "Adaptive Learning Rate" optimization algorithm?**
* A) Vanilla SGD
* B) Adam
* C) Batch Gradient Descent
* D) Ordinary Least Squares

**79. What defines a dataset as "Imbalanced"?**
* A) The number of features exceeds the number of rows.
* B) The numerical features are not standardized.
* C) The target classes are not represented equally (e.g., 99% Class A, 1% Class B).
* D) The dataset has an odd number of rows.

**80. In an imbalanced classification scenario, why is standard "Accuracy" a deceptive metric?**
* A) It cannot compute properly.
* B) A model can simply guess the majority class 100% of the time and still achieve a deceptively high accuracy score (e.g., 99%), despite failing completely at its actual predictive task.
* C) Accuracy requires the loss function to be exactly zero.
* D) Accuracy automatically overfits the minority class.

**81. What is the primary purpose of applying an Activation Function to the input space before passing it to the next layer?**
* A) To multiply the weights by the learning rate.
* B) To convert continuous numbers into integers.
* C) To map the input feature space into a new, often non-linear representation space, making it easier for subsequent layers to draw linear decision boundaries.
* D) To execute the backward propagation of error.

**82. When a Neural Network is referred to as "Deep," it specifically implies:**
* A) A high number of neurons in a single layer.
* B) The presence of multiple hidden layers stacked sequentially between the input and output.
* C) The use of complex activation functions like Tanh.
* D) The application of L2 regularization.

**83. If a network uses a Softmax activation function on an output layer of 3 neurons, and the raw pre-activation logits are `[1.0, 1.0, 1.0]`, what will the Softmax output array be?**
* A) `[1.0, 1.0, 1.0]`
* B) `[0.0, 0.0, 0.0]`
* C) `[0.33, 0.33, 0.33]`
* D) `[0.5, 0.5, 0.5]`

**84. Why are Recurrent Neural Networks (RNNs) uniquely suited for Natural Language Processing (NLP) or time-series tasks?**
* A) They do not require an output layer.
* B) They possess internal loops that allow information to persist, enabling them to process sequences of varying lengths while maintaining chronological context.
* C) They use completely linear activation functions.
* D) They evaluate the entire dataset in a single matrix inversion.

**85. What is "Data Leakage" in the context of training neural networks?**
* A) When memory overflows on the GPU.
* B) When information from the Test or Validation set is accidentally utilized during the Training phase, leading to artificially inflated performance metrics.
* C) When weights become extremely small and "leak" towards zero.
* D) When dropout layers are configured incorrectly.

**86. A network is predicting the continuous price of a stock. The evaluation metrics yield an MSE of 100. What is the Root Mean Squared Error (RMSE), and what is its unit?**
* A) 10000; squared dollars
* B) 10; dollars
* C) 10; squared dollars
* D) 50; dollars

**87. Which component of the neural network determines the maximum limit on the complexity of the functions it can approximate?**
* A) The Batch Size
* B) The number of training epochs
* C) The architecture capacity (number of layers and neurons)
* D) The Learning Rate

**88. The concept of a "Decision Boundary" in a neural network classifier refers to:**
* A) The maximum memory allocation limit.
* B) The mathematical hyperplane or surface dividing the feature space into distinct regions corresponding to different class labels.
* C) The maximum value of the bias term.
* D) The final epoch of training.

**89. If an artificial neuron possesses 5 inputs, how many corresponding weights does it have connecting to those inputs?**
* A) 1
* B) 5
* C) 6 (including bias)
* D) 10

**90. What specific historical event is primarily attributed to Minsky and Papert's publication detailing the XOR limitation of perceptrons?**
* A) The immediate invention of the CNN.
* B) The first "AI Winter," characterized by a massive reduction in funding and research for neural networks.
* C) The development of the Turing Test.
* D) The widespread adoption of Support Vector Machines.

**91. In a classification report, the "Support" column indicates:**
* A) The confidence interval of the prediction.
* B) The number of epochs used to train that specific class.
* C) The actual, true number of occurrences of that specific class in the evaluation dataset.
* D) The mathematical weight assigned to the L2 regularization penalty.

**92. How does an "Autoencoder" neural network fundamentally operate?**
* A) It is trained to predict the next word in a sequence.
* B) It is an unsupervised architecture trained to compress the input into a lower-dimensional latent space and then reconstruct the original input as accurately as possible at the output layer.
* C) It automatically encodes categorical string labels into numerical arrays.
* D) It searches for the optimal hyperparameters without human intervention.

**93. Which technique involves randomly splitting the training data into 'K' distinct subsets, training the model 'K' times using 'K-1' subsets for training and the remaining subset for validation?**
* A) K-Means Clustering
* B) K-Fold Cross-Validation
* C) Batch Gradient Descent
* D) Early Stopping

**94. In neural network terminology, what does "Logits" refer to?**
* A) The logarithmic error function.
* B) The final output probabilities.
* C) The raw, unscaled, un-normalized outputs of the network's final layer *before* an activation function (like Softmax) is applied.
* D) The binary classification outputs.

**95. A "Confusion Matrix" is an evaluation tool specifically used to:**
* A) Track the fluctuation of the learning rate.
* B) Provide a tabular breakdown of a classification model's performance, explicitly showing True Positives, True Negatives, False Positives, and False Negatives.
* C) Determine the optimal number of hidden layers.
* D) Confuse adversarial networks.

**96. If you want to force your neural network's weights to remain extremely small to prevent it from relying too heavily on any single feature, you should apply:**
* A) A ReLU activation function
* B) A very high Learning Rate
* C) L2 Regularization (Weight Decay)
* D) Stochastic Gradient Descent with a batch size of 1

**97. What is the fundamental mathematical relationship between the Learning Rate and the Gradient during weight updates?**
* A) They are added together.
* B) They are divided.
* C) The Learning Rate acts as a scalar multiplier to the Gradient, determining the physical "step size" taken towards the minimum.
* D) They are strictly independent mathematical concepts.

**98. Why are CNNs (Convolutional Neural Networks) preferred over standard Dense MLPs for analyzing high-resolution images?**
* A) Dense MLPs require "flattening" the image, destroying vital 2D spatial relationships, and result in an unmanageably massive number of weight parameters.
* B) CNNs are programmed in specialized visual languages.
* C) Dense MLPs cannot process RGB color data.
* D) CNNs completely bypass the need for backpropagation optimization.

**99. If a multi-layer neural network uses exclusively step (threshold) activation functions, why will standard backpropagation instantly fail?**
* A) The network will consume too much memory.
* B) The gradient (derivative) of a step function is zero almost everywhere, meaning no error signal can propagate backwards to update the weights.
* C) Step functions restrict the network to 1 hidden layer.
* D) Step functions force the loss to be negative.

**100. Which fundamental property of Neural Networks allows them to excel in areas like facial recognition and natural language translation compared to classical algorithms like simple linear regression?**
* A) Their ability to execute without any training data.
* B) Their inherent capacity to learn and model incredibly complex, high-dimensional, highly non-linear feature representations hierarchically.
* C) Their reliance on strictly structured SQL databases.
* D) Their ability to be trained using exact analytical matrix inversions rather than iterative optimization.

---

### **Answers: Questions 1 – 50**

| | | | | | | | | | |
|---|---|---|---|---|---|---|---|---|---|
| **1.** B | **2.** C | **3.** B | **4.** C | **5.** B | **6.** B | **7.** A | **8.** B | **9.** A | **10.** C |
| **11.** C | **12.** B | **13.** B | **14.** D | **15.** C | **16.** A | **17.** B | **18.** B | **19.** C | **20.** B |
| **21.** C | **22.** B | **23.** B | **24.** D | **25.** C | **26.** B | **27.** B | **28.** C | **29.** B | **30.** B |
| **31.** C | **32.** B | **33.** B | **34.** C | **35.** C | **36.** B | **37.** A | **38.** C | **39.** C | **40.** D |
| **41.** C | **42.** B | **43.** B | **44.** B | **45.** C | **46.** D | **47.** B | **48.** B | **49.** B | **50.** C |

<br>

### **Answers: Questions 51 – 100**

| | | | | | | | | | |
|---|---|---|---|---|---|---|---|---|---|
| **51.** C | **52.** C | **53.** B | **54.** D | **55.** B | **56.** C | **57.** B | **58.** B | **59.** C | **60.** B |
| **61.** B | **62.** C | **63.** B | **64.** C | **65.** B | **66.** C | **67.** B | **68.** C | **69.** C | **70.** C |
| **71.** B | **72.** A | **73.** B | **74.** A | **75.** B | **76.** B | **77.** C | **78.** B | **79.** C | **80.** B |
| **81.** C | **82.** B | **83.** C | **84.** B | **85.** B | **86.** B | **87.** C | **88.** B | **89.** B | **90.** B |
| **91.** C | **92.** B | **93.** B | **94.** C | **95.** B | **96.** C | **97.** C | **98.** A | **99.** B | **100.** B |