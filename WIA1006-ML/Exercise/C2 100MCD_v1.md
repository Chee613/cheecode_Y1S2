# Chapter 2: Data Preprocessing and Regression Analysis - Advanced MCQ Assessment

**1. When handling missing data in a dataset containing a highly skewed numerical feature, which imputation strategy is generally most robust against the influence of extreme values?**
* A) Mean imputation
* B) Median imputation
* C) Mode imputation
* D) Forward-fill imputation

**2. What is the primary mathematical distinction between Min-Max Normalization and Z-score Standardization?**
* A) Normalization bounds data strictly between 0 and 1, whereas Standardization centers data around a mean of 0 with a standard deviation of 1.
* B) Standardization bounds data strictly between -1 and 1, whereas Normalization centers data around the median.
* C) Normalization is robust to outliers, while Standardization is highly sensitive to them.
* D) Standardization scales the variance to match the interquartile range, while Normalization scales the mean.

**3. In Multiple Linear Regression, what phenomenon occurs when two or more predictor variables are highly linearly related?**
* A) Heteroscedasticity
* B) Multicollinearity
* C) Autocorrelation
* D) Polynomial confounding

**4. Why is the "Dummy Variable Trap" a significant issue when applying One-Hot Encoding to categorical variables in linear regression models?**
* A) It introduces categorical variables into an algorithm that only accepts continuous variables.
* B) It causes perfect multicollinearity by creating a newly engineered feature that is a perfect linear combination of the others.
* C) It arbitrarily increases the learning rate of the gradient descent algorithm, causing divergence.
* D) It prevents the use of the Mean Squared Error (MSE) loss function.

**5. How is the Adjusted R-squared metric mathematically superior to the standard R-squared metric when evaluating a Multiple Linear Regression model?**
* A) Adjusted R-squared penalizes the model for adding independent variables that do not significantly improve the model's explanatory power.
* B) Adjusted R-squared ignores the residual sum of squares and only focuses on the total sum of squares.
* C) Adjusted R-squared always strictly increases as more features are added, providing a clear ceiling.
* D) Adjusted R-squared measures the absolute error rather than the squared error.

**6. Which of the following best describes the core objective of the Gradient Descent algorithm in the context of Linear Regression?**
* A) To maximize the R-squared value by iteratively adding polynomial features.
* B) To find the global minimum of the cost function by iteratively updating the model parameters (weights and bias) in the opposite direction of the gradient.
* C) To analytically solve for the optimal weights using the Normal Equation matrix operations.
* D) To randomly initialize weights until the Mean Absolute Error reaches zero.

**7. When applying Polynomial Regression, what is the fundamental reason it is still considered a subset of "Linear" Regression?**
* A) The relationship between the independent variable 'x' and dependent variable 'y' forms a straight line on a graph.
* B) The algorithm utilizes a linear activation function at the output layer.
* C) The model remains linear with respect to its unknown parameters (coefficients), even though it is non-linear with respect to the input features.
* D) Polynomial terms are only applied to the dependent variable, leaving the independent features strictly linear.

**8. If a dataset contains a categorical feature "T-shirt Size" with values [Small, Medium, Large, Extra Large], which encoding technique is most appropriate to preserve the inherent ordinal relationship?**
* A) One-Hot Encoding
* B) Binary Encoding
* C) Label Encoding (or Ordinal Encoding)
* D) Frequency Encoding

**9. What is the impact of outlier values on a model evaluated primarily using Mean Squared Error (MSE) compared to Mean Absolute Error (MAE)?**
* A) MSE completely ignores outliers, whereas MAE emphasizes them.
* B) MSE penalizes large errors (outliers) much more heavily than MAE due to the squaring of the error terms.
* C) MSE and MAE react identically to outliers, but MSE is computationally faster.
* D) MAE squares the absolute differences, making it infinitely more sensitive to outliers than MSE.

**10. In the equation for Simple Linear Regression ($y = \beta_0 + \beta_1x + \epsilon$), what does the $\epsilon$ term represent?**
* A) The slope of the regression line.
* B) The y-intercept when x is zero.
* C) The irreducible error or residual term capturing unobserved factors.
* D) The learning rate of the gradient descent optimization.

**11. Which assumption of Linear Regression states that the variance of the error terms must be constant across all levels of the independent variables?**
* A) Linearity
* B) Independence of Errors
* C) Homoscedasticity
* D) Normal Distribution of Errors

**12. When splitting a dataset into training and testing sets, what is the primary risk of not shuffling a chronologically ordered dataset beforehand?**
* A) The learning rate will fail to converge.
* B) The training and testing sets may end up with vastly different distributions, destroying the model's ability to generalize.
* C) The categorical variables will lose their one-hot encoded state.
* D) The R-squared value will default to exactly 1.0.

**13. In a regression context, if a model has an R-squared value of 0, what does this practically signify?**
* A) The model perfectly predicts all data points without any error.
* B) The model performs exactly as well as a horizontal line drawn through the mean of the dependent variable.
* C) The model's predictions are infinitely far from the true values.
* D) The independent variables explain 100% of the variance in the dependent variable.

**14. What occurs if the learning rate ($\alpha$) in a Gradient Descent algorithm is set too high?**
* A) The algorithm will converge far too slowly, taking millions of unnecessary epochs.
* B) The algorithm may overshoot the global minimum, failing to converge and potentially diverging entirely.
* C) The cost function will automatically switch to a Mean Absolute Error calculation.
* D) The algorithm will perfectly memorize the training data, leading to instant overfitting.

**15. Why is Feature Scaling (like Standardization) considered a crucial preprocessing step before applying Gradient Descent?**
* A) It allows gradient descent to converge more quickly and uniformly by ensuring all features contribute proportionally to the cost function's contour.
* B) It transforms categorical text data into numerical vectors that the algorithm can process.
* C) It strictly bounds the output predictions between 0 and 1.
* D) It permanently eliminates outliers from the dataset.

**16. If you have a feature "City" with 500 unique categories, why is standard One-Hot Encoding likely a poor choice?**
* A) It will assume an artificial numerical order among the cities.
* B) It will drastically expand the dimensionality of the dataset (creating 500 new columns), potentially leading to the curse of dimensionality and memory issues.
* C) It will force the dependent variable to become categorical as well.
* D) It is mathematically impossible to one-hot encode more than 10 categories simultaneously.

**17. What is the primary difference between identifying an outlier and identifying an influential point in a regression dataset?**
* A) Outliers are strictly found in categorical data, whereas influential points exist in continuous data.
* B) An outlier has an extreme y-value, whereas an influential point significantly alters the slope of the regression line if removed.
* C) Outliers always improve model accuracy, while influential points always degrade it.
* D) There is no difference; the terms are perfectly synonymous in statistics.

**18. Which of the following evaluation metrics is strictly bounded between 0 and 1 (assuming a reasonable model fit on the training data)?**
* A) Mean Squared Error (MSE)
* B) Root Mean Squared Error (RMSE)
* C) Mean Absolute Error (MAE)
* D) R-squared ($R^2$)

**19. How do you resolve the Dummy Variable Trap when applying One-Hot Encoding for a categorical feature with 'k' categories?**
* A) By duplicating the column 'k' times.
* B) By dropping exactly one of the 'k' newly generated dummy columns, resulting in 'k-1' columns.
* C) By replacing the 'k' categories with their overall mean value.
* D) By applying a standard scalar to the categorical outputs.

**20. In the context of missing data, what does MCAR stand for?**
* A) Missing Completely At Random
* B) Missing Categorical Attributes Repeatedly
* C) Mean Calculation And Regression
* D) Mode Correlated Artificial Reduction

**21. If you are building a Polynomial Regression model to fit a U-shaped curve, what is the minimum polynomial degree you must include?**
* A) Degree 1
* B) Degree 2
* C) Degree 3
* D) Degree 4

**22. Which algorithm analytically computes the exact parameters ($\beta$) for Linear Regression without requiring an iterative optimization process like Gradient Descent?**
* A) The Backpropagation Algorithm
* B) The Normal Equation
* C) The Random Forest Regressor
* D) The K-Means clustering algorithm

**23. What is the mathematical definition of the Residual ($e_i$) for a single data point in regression?**
* A) The actual value ($y_i$) plus the predicted value ($\hat{y}_i$).
* B) The actual value ($y_i$) divided by the predicted value ($\hat{y}_i$).
* C) The actual value ($y_i$) minus the predicted value ($\hat{y}_i$).
* D) The predicted value ($\hat{y}_i$) squared.

**24. When standardizing a feature, what is the formula to calculate the Z-score for a specific value $x$?**
* A) $Z = (x - \text{mean}) / \text{variance}$
* B) $Z = (x - \text{mean}) / \text{standard deviation}$
* C) $Z = (x - \text{min}) / (\text{max} - \text{min})$
* D) $Z = (x - \text{median}) / \text{standard deviation}$

**25. If a feature represents "Salary" and ranges from \$20,000 to \$5,000,000, what preprocessing technique is highly recommended to reduce the skewness of this heavy-tailed distribution?**
* A) Label Encoding
* B) Mode Imputation
* C) Logarithmic Transformation
* D) One-Hot Encoding

**26. In a dataset forecasting house prices, if "Number of Bedrooms" and "Number of Bathrooms" have a correlation coefficient of 0.95, what problem does this present for Multiple Linear Regression?**
* A) High bias
* B) Multicollinearity
* C) Underfitting
* D) Homoscedasticity

**27. What is the interpretation of a negative coefficient ($\beta_1$) in a Simple Linear Regression model?**
* A) As the independent variable increases, the dependent variable decreases.
* B) As the independent variable increases, the dependent variable increases at a slower rate.
* C) The model has generated an invalid mathematical state.
* D) The intercept is positioned below the x-axis.

**28. Which of the following is a disadvantage of dropping rows with missing values (Listwise Deletion)?**
* A) It arbitrarily inflates the variance of the remaining data.
* B) It can lead to a significant loss of valuable information and reduce the statistical power of the model.
* C) It forces the model to use Mean Absolute Error instead of Mean Squared Error.
* D) It inherently causes multicollinearity among the remaining features.

**29. What is "Data Leakage" during the preprocessing phase?**
* A) When categorical variables fail to encode properly, leaking text strings into the model.
* B) When information from outside the training dataset (e.g., from the test set) is used to create the model, leading to overly optimistic performance estimates.
* C) When the gradient descent algorithm overshoots the minimum and leaks weights.
* D) When missing values are imputed with zeros instead of the mean.

**30. Why must you fit your Standard Scaler ONLY on the training data, and then apply it to both the training and testing data?**
* A) Because the testing data does not require scaling.
* B) To prevent data leakage; calculating the mean and standard deviation across the entire dataset would give the training process hidden knowledge about the test set.
* C) Because standard scaling mathematically fails if applied to test data independently.
* D) To ensure that the test set has a variance of exactly 0.

**31. Which of the following scenarios describes "Underfitting" in a regression model?**
* A) The model captures the noise in the training data and performs poorly on test data.
* B) The model is too simple to capture the underlying underlying patterns in the data, performing poorly on both training and test data.
* C) The model relies on too many polynomial degrees.
* D) The gradient descent algorithm uses a learning rate that is too small.

**32. What is a common visual tool used to intuitively check for the assumption of Homoscedasticity?**
* A) A bar chart of categorical variables.
* B) A scatter plot of Residuals versus Predicted values.
* C) A pie chart of target variables.
* D) A correlation heatmap of independent variables.

**33. How does Root Mean Squared Error (RMSE) differ from Mean Squared Error (MSE) in terms of interpretability?**
* A) RMSE is interpreted as a percentage, while MSE is a raw number.
* B) RMSE transforms the error back into the original units of the target variable, making it easier to interpret than the squared units of MSE.
* C) RMSE ignores outliers, making it a "cleaner" interpretation.
* D) There is no difference in interpretability; they represent the exact same value.

**34. If a regression model attempts to fit a dataset spanning 100 years of stock prices using a simple straight line, what specific error is likely dominating?**
* A) High Variance
* B) High Bias
* C) Perfect Collinearity
* D) Zero Residuals

**35. What does the term "Feature Engineering" represent in the preprocessing pipeline?**
* A) Deleting all rows with missing values to engineer a clean dataset.
* B) Translating the model's output code from Python to C++.
* C) The process of using domain knowledge to extract or create new variables from raw data that make machine learning algorithms work better.
* D) Using gradient descent to scale features automatically.

**36. In Multiple Linear Regression, what is the role of the "Intercept" ($\beta_0$)?**
* A) It represents the predicted value of the dependent variable when all independent variables are precisely equal to zero.
* B) It is the specific learning rate assigned to the first feature.
* C) It dictates the maximum possible error of the model.
* D) It represents the slope of the steepest feature.

**37. Which preprocessing technique is absolutely necessary before feeding categorical string labels (e.g., "Red", "Blue") into a standard mathematical regression model?**
* A) Z-score Standardization
* B) Min-Max Normalization
* C) Numerical Encoding (e.g., Label Encoding or One-Hot Encoding)
* D) Mean Imputation

**38. What is the fundamental goal of finding the "Line of Best Fit" in Ordinary Least Squares (OLS) regression?**
* A) To maximize the distance between the line and the furthest outlier.
* B) To minimize the sum of the squared residuals between the predicted values on the line and the actual data points.
* C) To ensure the line passes exactly through the origin (0,0).
* D) To force the R-squared value to drop below zero.

**39. When evaluating a regression model, if the MAE is 10 and the RMSE is 50, what does this significantly large gap imply?**
* A) The model has a low bias but a remarkably high learning rate.
* B) The dataset likely contains very large outliers, as RMSE squares the errors, disproportionately highlighting large discrepancies compared to MAE.
* C) The dependent variable is strictly categorical.
* D) The model is suffering from the dummy variable trap.

**40. If an independent variable has a variance of 0 (a constant value across all rows), what happens if you include it in a linear regression model?**
* A) It becomes the most important feature with the highest coefficient.
* B) It provides no predictive power and can cause mathematical singularities (division by zero) when calculating matrix inversions or scaling.
* C) It automatically scales the target variable to 0.
* D) It acts as a perfect substitute for the intercept.

**41. Which of the following is NOT a standard method for identifying outliers in a numerical dataset?**
* A) Z-score thresholding (e.g., $|Z| > 3$)
* B) Interquartile Range (IQR) method
* C) One-Hot Encoding Analysis
* D) Boxplot visualization

**42. In a 2D space, what does the gradient of the cost function represent at any specific point?**
* A) The global minimum.
* B) The direction of the steepest ascent (maximum increase) of the cost function.
* C) The final optimal weight of the model.
* D) The Mean Absolute Error at that point.

**43. Why is Gradient Descent heavily reliant on Feature Scaling?**
* A) Unscaled features cause the cost function contour to be highly elliptical, causing gradient descent to oscillate inefficiently and converge slowly.
* B) Scaling prevents the dataset from containing categorical text strings.
* C) Unscaled features inherently violate the linearity assumption.
* D) Gradient descent cannot compute derivatives on integers.

**44. What defines a "Non-linear Regression" model fundamentally differently from Polynomial Regression?**
* A) Non-linear regression cannot use the MSE loss function.
* B) In non-linear regression, the model is non-linear with respect to its *parameters* (e.g., $y = \beta_0 \cdot e^{\beta_1 x}$), not just its features.
* C) Polynomial regression only deals with categorical variables.
* D) Non-linear regression forces R-squared to be 1.

**45. What does the 'fit_transform()' method typically do in libraries like Scikit-Learn during preprocessing?**
* A) It fits the model to the test data and transforms the training data.
* B) It computes the necessary parameters (like mean/std deviation) from the data and simultaneously applies the transformation to that same data.
* C) It drops missing values and fills them with the mean.
* D) It converts a regression model into a classification model.

**46. Which of the following correlation coefficients indicates the strongest linear relationship between two variables?**
* A) 0.05
* B) -0.45
* C) 0.88
* D) -0.92

**47. When using the K-Nearest Neighbors (KNN) algorithm for imputation of missing values, how does the algorithm determine the replacement value?**
* A) By calculating the global mean of the entire dataset.
* B) By finding the 'k' most similar data points (based on other available features) and taking the average (or majority vote) of their values.
* C) By randomly sampling from a normal distribution.
* D) By assigning the value of zero to all missing fields.

**48. Why is extrapolation generally dangerous in Linear Regression?**
* A) It forces the residuals to become negative.
* B) It involves predicting values far outside the range of the training data, relying on the unverified assumption that the linear relationship holds indefinitely.
* C) It arbitrarily drops essential features from the model.
* D) It converts categorical predictions into continuous ones.

**49. In the context of a cost function curve, what does a "convex" function guarantee for Gradient Descent?**
* A) That the algorithm will diverge infinitely.
* B) That there are multiple local minima to choose from.
* C) That any local minimum found is also the global minimum.
* D) That the learning rate must be exactly 1.0.

**50. What is a specific advantage of using the Mean Absolute Error (MAE) over the Mean Squared Error (MSE)?**
* A) MAE is easily differentiable at exactly zero.
* B) MAE is more robust to outliers because it does not square the error terms.
* C) MAE is strictly bounded between 0 and 100.
* D) MAE automatically adjusts for the number of features in a multiple linear regression model.

**51. Which mathematical operation does the Normal Equation $\theta = (X^T X)^{-1} X^T y$ utilize that makes it computationally expensive for datasets with massive numbers of features?**
* A) Vector addition
* B) Scalar multiplication
* C) Matrix inversion
* D) Matrix transposition

**52. If your Multiple Linear Regression model yields an R-squared of 0.95 but an Adjusted R-squared of 0.40, what is the most likely diagnosis?**
* A) The model is severely underfitting the data.
* B) The model contains a massive number of irrelevant features that artificially inflated the standard R-squared.
* C) The target variable requires a log transformation.
* D) The learning rate was set too low.

**53. How does "Stochastic Gradient Descent" (SGD) differ from "Batch Gradient Descent"?**
* A) SGD processes the entire dataset before making a single parameter update.
* B) SGD updates the parameters for every single training example one by one, leading to a faster but noisier convergence path.
* C) SGD does not use a learning rate.
* D) SGD analytically solves the cost function without iteration.

**54. In data preprocessing, what is "Binarization"?**
* A) Converting continuous numerical values into 0 or 1 based on a strict threshold.
* B) Dropping all features except for two.
* C) Using only two epochs in gradient descent.
* D) Encoding the target variable into a normal distribution.

**55. What is the primary purpose of the `train_test_split` procedure?**
* A) To double the size of the dataset.
* B) To test the model on the exact same data it learned from to verify its memorization capability.
* C) To create a separate, unseen subset of data to objectively evaluate how well the model generalizes to new information.
* D) To sort the data chronologically before scaling.

**56. If a model predicts house prices and the predictions are consistently exactly \$50,000 higher than the true prices, this systematic error indicates a problem with the model's:**
* A) Variance
* B) Outlier threshold
* C) Bias (specifically the intercept term)
* D) Categorical encoding

**57. What is "Heteroscedasticity" in residual analysis?**
* A) The residuals form a perfect horizontal line.
* B) The variance of the residuals remains constant across all values of the independent variable.
* C) The variance of the residuals changes or fans out as the independent variable changes (e.g., a cone shape in a scatter plot).
* D) The residuals are strictly positive.

**58. Why might replacing missing values with the "Mean" be detrimental if the data contains massive outliers?**
* A) The mean is heavily skewed by outliers, resulting in an imputed value that does not represent the central tendency of the majority of the data.
* B) Mean imputation is mathematically impossible if negative numbers exist.
* C) Outliers cause the mean to evaluate to zero.
* D) Mean imputation forces the data into a categorical format.

**59. In the formula for R-squared ($R^2 = 1 - \frac{RSS}{TSS}$), what does TSS (Total Sum of Squares) represent?**
* A) The total error of the regression line.
* B) The sum of the squared differences between the actual data points and the overall mean of the dependent variable (the baseline model).
* C) The total number of parameters in the model.
* D) The sum of the absolute errors.

**60. Which of the following best defines "Epoch" in the context of training a machine learning model?**
* A) A single matrix inversion calculation.
* B) The process of deleting one outlier from the dataset.
* C) One complete pass of the entire training dataset through the learning algorithm.
* D) The mathematical limit as the learning rate approaches zero.

**61. What does the "Curse of Dimensionality" refer to in the context of data preprocessing and modeling?**
* A) The fact that 3D plots are difficult to render.
* B) As the number of features increases, the volume of the data space grows exponentially, making the available data sparse and causing distance-based metrics to lose meaning.
* C) The inability to use Gradient Descent on datasets with more than 3 dimensions.
* D) The requirement to explicitly drop the intercept term when features exceed 100.

**62. If you apply a Polynomial degree of 15 to a simple dataset of 10 points, what is the most guaranteed outcome?**
* A) Severe Underfitting
* B) Severe Overfitting
* C) Perfect generalized forecasting
* D) The algorithm will convert to a classification model.

**63. Which normalization technique is most appropriate when you strictly need the feature values to be bounded between a specific range, such as [0, 1]?**
* A) Z-Score Standardization
* B) Min-Max Scaling
* C) Log Transform
* D) Robust Scaler (using IQR)

**64. When performing Multiple Linear Regression, which metric is commonly used to mathematically quantify and check for Multicollinearity among features?**
* A) Adjusted R-squared
* B) Variance Inflation Factor (VIF)
* C) Mean Absolute Error (MAE)
* D) Z-score

**65. What happens to the Cost Function value as the Gradient Descent algorithm approaches the local/global minimum?**
* A) It rises exponentially.
* B) It oscillates violently between negative and positive infinity.
* C) The slope of the gradient approaches zero, causing the parameter updates (steps) to become smaller and the cost to plateau.
* D) It drops immediately to exactly zero in one step.

**66. Why is it dangerous to impute missing values in the Test set using the mean calculated from the Test set itself?**
* A) The test set mean is always zero.
* B) It violates the principle of using the test set as strictly unseen data; the test set must be transformed using parameters learned *only* from the training set.
* C) The test set will automatically overfit the model.
* D) It causes the categorical variables to decode themselves.

**67. In a regression formula $\hat{y} = \beta_0 + \beta_1 x_1 + \beta_2 x_2^2$, is this model considered a Linear Regression model?**
* A) No, because $x_2$ is squared.
* B) Yes, because it is linear with respect to the parameters ($\beta_0, \beta_1, \beta_2$).
* C) No, because there are two independent variables.
* D) Yes, because the output $\hat{y}$ is a discrete class.

**68. What is the fundamental difference between a parameter and a hyperparameter in regression algorithms?**
* A) Parameters are set by the user before training (e.g., learning rate), while hyperparameters are learned from the data during training (e.g., weights).
* B) Hyperparameters are set by the user before training (e.g., learning rate), while parameters are learned from the data during training (e.g., weights).
* C) Hyperparameters only exist in classification, not regression.
* D) They are identical terms used interchangeably in preprocessing.

**69. Which of the following transformations is typically used to handle a dataset with a heavy right-skewed (positive skew) distribution?**
* A) Exponential transformation
* B) Logarithmic transformation
* C) Squaring the data
* D) Min-Max scaling

**70. What defines "Data Imputation" in the preprocessing pipeline?**
* A) The process of completely removing rows that contain NaN values.
* B) The process of replacing missing data with substituted values (e.g., mean, median, predicted values).
* C) The process of scaling data to fit a normal distribution.
* D) The process of converting text to numerical codes.

**71. How does standard Linear Regression handle categorical target variables (dependent variables)?**
* A) It maps them to a continuous probability curve automatically.
* B) Standard Linear Regression is specifically designed for continuous numerical target variables, not categorical targets (which require Logistic Regression or classification algorithms).
* C) It applies one-hot encoding to the target variable and runs multiple models.
* D) It drops the target variable and runs as an unsupervised model.

**72. What does a Correlation Coefficient of 0 imply about two continuous variables?**
* A) They have a perfect positive linear relationship.
* B) They have a perfect negative linear relationship.
* C) There is no linear relationship between them.
* D) They are identically equal to each other.

**73. Which optimization technique uses random "mini-batches" of data to update the model weights, balancing speed and stability?**
* A) Batch Gradient Descent
* B) Stochastic Gradient Descent (SGD)
* C) Mini-batch Gradient Descent
* D) The Normal Equation

**74. When evaluating Residuals, what does a normal distribution of residuals centered around zero suggest?**
* A) The model is suffering from heavy multicollinearity.
* B) The linear regression model is appropriate and has captured the systematic information in the data.
* C) The model is severely underfitting.
* D) The learning rate was too high.

**75. What is the main advantage of the "Robust Scaler" (which uses the median and interquartile range) over standard Z-score Standardization?**
* A) It computes much faster on large datasets.
* B) It is not influenced by a few extreme marginal outliers.
* C) It bounds the data strictly between 0 and 1.
* D) It automatically imputes missing categorical data.

**76. In an equation where $Price = 1000 + 50(Area) - 200(Age)$, what is the predicted price for a 10-year-old house with an area of 100?**
* A) 3000
* B) 4000
* C) 5000
* D) 6000

**77. If you apply a log transformation to your target variable $y$ before training a linear regression model, what must you do to your final predictions to interpret them in the original scale?**
* A) Divide by 10.
* B) Apply the exponential function ($e^{\hat{y}}$) to the predictions.
* C) Multiply by the mean of $y$.
* D) Standardize the predictions.

**78. What does "Dimensionality Reduction" aim to achieve in preprocessing?**
* A) Increasing the number of features by multiplying them together.
* B) Reducing the number of random variables under consideration, obtaining a set of principal variables, mitigating the curse of dimensionality.
* C) Reducing the number of rows (data points) in the dataset to speed up training.
* D) Dropping the dependent target variable.

**79. Which metric is the most appropriate loss function for training an Ordinary Least Squares (OLS) Linear Regression model?**
* A) Accuracy
* B) F1-Score
* C) Mean Squared Error (MSE)
* D) Cross-Entropy Loss

**80. What is a "Baseline Model" in the context of evaluating a regression task?**
* A) A complex polynomial model used as the ultimate goal.
* B) The simplest possible model (like always predicting the mean of the target variable) used as a point of comparison to see if the trained algorithm adds actual value.
* C) A model that predicts 0 for every input.
* D) The test dataset itself.

**81. If a categorical column "Color" has values [Red, Green, Blue], how many columns will be created using standard One-Hot Encoding (without dropping any to avoid the dummy trap)?**
* A) 1
* B) 2
* C) 3
* D) 4

**82. Why is "Label Encoding" considered risky for nominal categorical variables like "Country" (e.g., $USA=1$, $France=2$, $Japan=3$)?**
* A) It causes the data to disappear from the dataset.
* B) Regression algorithms will interpret these numbers mathematically, assuming Japan (3) is "greater than" or "three times" USA (1), which is false.
* C) It limits the learning rate to a maximum of 3.
* D) It requires the target variable to also be a nominal category.

**83. What is the effect of having an extremely small Learning Rate ($\alpha$) in Gradient Descent?**
* A) The model will diverge and the cost will reach infinity.
* B) The algorithm will take tiny steps and may take an impractically long time to converge to the minimum.
* C) The algorithm will jump completely over the global minimum.
* D) It forces the cost function to become non-convex.

**84. In Simple Linear Regression, the formula for the slope $\beta_1$ can be expressed analytically using which statistical components?**
* A) The sum of all y-values divided by the sum of all x-values.
* B) The covariance of x and y divided by the variance of x.
* C) The median of x divided by the median of y.
* D) The log of x times the log of y.

**85. What does an R-squared value of exactly 1.0 imply?**
* A) The model explains none of the variance in the target variable.
* B) The model perfectly explains 100% of the variance in the target variable; every predicted point lies exactly on the true data point.
* C) The model has a Mean Squared Error of 1.0.
* D) The dependent variable is totally random.

**86. When visualizing a dataset with a scatter plot prior to regression, you notice the data points follow a distinct "S" shape. What model adjustment is required?**
* A) Apply a Standard Scaler to force the points into a straight line.
* B) Introduce polynomial terms (e.g., $x^2$, $x^3$) to allow the regression line to curve and fit the non-linear shape.
* C) Drop all data points that don't fit a straight line.
* D) Switch to an unsupervised clustering algorithm.

**87. What is "Forward Selection" in the context of Multiple Linear Regression?**
* A) A technique to fill missing values by carrying the last known value forward.
* B) A feature selection method that starts with an empty model and adds variables one by one based on which improves the model most, until no further improvement is seen.
* C) Updating the weights of the neural network from left to right.
* D) Extrapolating predictions into the future.

**88. Why does the Normal Equation $\theta = (X^T X)^{-1} X^T y$ become practically unusable for datasets with millions of features?**
* A) It cannot calculate negative numbers.
* B) The computational complexity of inverting the matrix $(X^T X)$ is roughly $O(n^3)$, making it impossibly slow or computationally catastrophic for massive dimensions.
* C) It relies on an extremely high learning rate.
* D) It requires all data to be categorized first.

**89. If the Cost Function $J(\theta)$ increases after an iteration of Gradient Descent, what is the most definitive cause?**
* A) The learning rate $\alpha$ is too high, causing the algorithm to step too far and overshoot the minimum.
* B) The learning rate $\alpha$ is too small.
* C) The features have been perfectly standardized.
* D) The dataset contains no missing values.

**90. Which of the following is the correct mathematical formulation for Mean Absolute Error (MAE)?**
* A) $\frac{1}{n} \sum (\hat{y}_i - y_i)^2$
* B) $\frac{1}{n} \sum |\hat{y}_i - y_i|$
* C) $\sqrt{\frac{1}{n} \sum (\hat{y}_i - y_i)^2}$
* D) $1 - \frac{\sum (y_i - \hat{y}_i)^2}{\sum (y_i - \bar{y})^2}$

**91. What is the fundamental assumption of the "Independence of Errors" in Linear Regression?**
* A) The independent variables must not be correlated with each other.
* B) The residuals (errors) should not follow any pattern or be correlated with one another (no autocorrelation).
* C) The target variable must be independent of the features.
* D) Missing values must be independently generated.

**92. In the context of the train/test split, what is the "Validation Set" commonly used for?**
* A) Replacing the training set entirely.
* B) Tuning hyperparameters (like the polynomial degree or learning rate) before making a final, unbiased evaluation on the test set.
* C) Storing dropped missing values.
* D) Unsupervised pre-training.

**93. What is "Overfitting" in a regression context?**
* A) When the model fails to learn anything and draws a horizontal line.
* B) When the model learns the training data's exact noise and fluctuations so closely that it fails to generalize to new, unseen data.
* C) When the learning rate is too slow.
* D) When the dataset is scaled too perfectly between 0 and 1.

**94. If a regression model has 5 independent variables, how many parameters (weights + bias) must the algorithm learn?**
* A) 4
* B) 5
* C) 6
* D) 10

**95. Which pandas/scikit-learn methodology is considered "best practice" to apply a series of preprocessing steps (like Imputation -> Scaling -> Modeling) systematically without data leakage?**
* A) Running individual `for` loops on the dataset.
* B) Using a `Pipeline` object.
* C) Manually editing the CSV file in Excel.
* D) Applying `.fit_transform()` repeatedly to the test data.

**96. What does it mean if the Adjusted R-squared is actually negative?**
* A) The model explains less variance than a horizontal line predicting the mean, and the penalty for useless features has driven the score below zero.
* B) The model has achieved perfect predictive capabilities but with a negative slope.
* C) The dataset has too few features.
* D) It is mathematically impossible; R-squared calculations are always strictly positive.

**97. In multiple linear regression, how do you interpret the coefficient $\beta_2$ for feature $x_2$?**
* A) The total change in $y$ for a one-unit change in $x_2$, ignoring all other variables.
* B) The change in the predicted value of $y$ for a one-unit change in $x_2$, assuming all other independent variables are held constant.
* C) The percentage change in $y$ divided by $x_2$.
* D) The maximum possible error caused by $x_2$.

**98. Why is simply replacing all NaN values with "0" generally considered a poor data preprocessing technique?**
* A) Zero is mathematically incompatible with gradient descent.
* B) Zero is an arbitrary number that rarely reflects the true central tendency of the missing data, heavily skewing the distribution and introducing artificial bias.
* C) Zero forces the matrix inversion in the Normal Equation to fail automatically.
* D) Zero converts numerical columns into categorical strings.

**99. Which graphical plot is best used to check if an independent continuous variable has a strictly linear relationship with the continuous target variable?**
* A) Box Plot
* B) Histogram
* C) Scatter Plot
* D) Bar Chart

**100. When calculating the Mean Squared Error (MSE), why are the error terms squared?**
* A) To artificially inflate the learning rate.
* B) To ensure that negative and positive errors do not cancel each other out, and to penalize larger errors more heavily.
* C) To convert the error into a percentage format.
* D) To force the error terms to follow a standard normal distribution.

---

### **Answers: Questions 1 – 50**

| | | | | | | | | | |
|---|---|---|---|---|---|---|---|---|---|
| **1.** B | **2.** A | **3.** B | **4.** B | **5.** A | **6.** B | **7.** C | **8.** C | **9.** B | **10.** C |
| **11.** C | **12.** B | **13.** B | **14.** B | **15.** A | **16.** B | **17.** B | **18.** D | **19.** B | **20.** A |
| **21.** B | **22.** B | **23.** C | **24.** B | **25.** C | **26.** B | **27.** A | **28.** B | **29.** B | **30.** B |
| **31.** B | **32.** B | **33.** B | **34.** B | **35.** C | **36.** A | **37.** C | **38.** B | **39.** B | **40.** B |
| **41.** C | **42.** B | **43.** A | **44.** B | **45.** B | **46.** D | **47.** B | **48.** B | **49.** C | **50.** B |

<br>

### **Answers: Questions 51 – 100**

| | | | | | | | | | |
|---|---|---|---|---|---|---|---|---|---|
| **51.** C | **52.** B | **53.** B | **54.** A | **55.** C | **56.** C | **57.** C | **58.** A | **59.** B | **60.** C |
| **61.** B | **62.** B | **63.** B | **64.** B | **65.** C | **66.** B | **67.** B | **68.** B | **69.** B | **70.** B |
| **71.** B | **72.** C | **73.** C | **74.** B | **75.** B | **76.** B | **77.** B | **78.** B | **79.** C | **80.** B |
| **81.** C | **82.** B | **83.** B | **84.** B | **85.** B | **86.** B | **87.** B | **88.** B | **89.** A | **90.** B |
| **91.** B | **92.** B | **93.** B | **94.** C | **95.** B | **96.** A | **97.** B | **98.** B | **99.** C | **100.** B |