# Chapter 4: Neural Networks - Advanced MCQ Assessment

**1. In the biological inspiration for Artificial Neural Networks, which component of a biological neuron is mathematically modeled by the "weights" assigned to inputs?**
* A) The Soma
* B) The Synapse
* C) The Dendrite
* D) The Axon

**2. According to the foundational McCulloch-Pitts neuron model, what specific restriction is placed on the state of the inputs and outputs?**
* A) They must be continuous real numbers normalized between 0 and 1.
* B) They are strictly binary, representing an "all-or-none" firing state.
* C) They must be standardized using a Z-score distribution.
* D) They represent complex numbers to handle phase shifts.

**3. Before passing through an activation function, what mathematical operation occurs inside the "node" (or soma) of a standard artificial neuron?**
* A) A logarithmic scaling of all inputs.
* B) A matrix inversion of the feature space.
* C) A weighted linear combination of the inputs plus a bias term.
* D) A non-linear feature mapping to a higher dimension.

**4. What is the primary geometric effect of adjusting the "bias" parameter in a single-layer perceptron?**
* A) It rotates the decision boundary around the origin.
* B) It shifts the decision boundary parallel to itself, away from or towards the origin.
* C) It changes the shape of the decision boundary from a straight line to a curve.
* D) It scales the magnitude of the normal weight vector.

**5. Which of the following boolean logic gates famously CANNOT be solved by a single-layer perceptron?**
* A) AND
* B) OR
* C) NAND
* D) XOR

**6. Who published the influential 1969 book "Perceptrons," which proved the severe limitations of single-layer networks and inadvertently contributed to the first "AI Winter"?**
* A) Marvin Minsky and Seymour Papert
* B) Frank Rosenblatt and Alan Turing
* C) Geoffrey Hinton and Yann LeCun
* D) Warren McCulloch and Walter Pitts

**7. In the context of training a Neural Network, what constitutes exactly one "Epoch"?**
* A) A single parameter update via gradient descent.
* B) The forward propagation of a single batch of data.
* C) One complete forward and backward pass of the entire training dataset through the network.
* D) Reaching the global minimum of the loss function.

**8. Consider a perceptron with two inputs ($x_1, x_2 \in \{0,1\}$), weights $w_1=0.5, w_2=0.5$, and a bias $b=-0.8$. If it uses a binary step activation function (firing if the sum $\ge 0$), which logic gate does it emulate?**
* A) OR
* B) AND
* C) NAND
* D) NOR

**9. To model a 2-input OR gate using a perceptron with a step function (fires if sum $\ge 0$), and weights $w_1=1, w_2=1$, which of the following is a valid bias ($b$)?**
* A) $b = -1.5$
* B) $b = -2.0$
* C) $b = 0.5$
* D) $b = -0.5$

**10. What defines a classification problem as "Linearly Separable"?**
* A) A single straight line (or hyperplane) can perfectly divide the dataset into its respective classes.
* B) The dataset can be separated using a polynomial curve of at least degree 2.
* C) The target variable is continuous and strictly linear.
* D) The loss function used is the Mean Squared Error.

**11. What happens if the Learning Rate ($\eta$) in a neural network is set excessively high during gradient descent?**
* A) The network will converge perfectly but at a much slower speed.
* B) The network will immediately overfit the training data and perfectly memorize the noise.
* C) The algorithm may overshoot the optimal minimum, potentially causing the loss to oscillate or diverge to infinity.
* D) The network architecture will automatically add more hidden layers to compensate.

**12. Why are Multi-Layer Perceptrons (MLPs) necessary for complex tasks like image recognition?**
* A) Single-layer networks can only learn linearly separable patterns, making them incapable of capturing the complex, non-linear boundaries required for advanced tasks.
* B) MLPs run much faster on modern CPU architectures than single-layer networks.
* C) Single-layer networks require the use of continuous activation functions, which degrades image data.
* D) MLPs completely eliminate the need for an activation function.

**13. The Backpropagation algorithm relies heavily on which fundamental concept from calculus?**
* A) Integration by parts
* B) The Chain Rule
* C) L'Hôpital's Rule
* D) The Fundamental Theorem of Calculus

**14. Which significant issue occurs in deep neural networks when using the standard Sigmoid activation function across many hidden layers?**
* A) The Exploding Gradient Problem
* B) The Curse of Dimensionality
* C) Perfect Multicollinearity
* D) The Vanishing Gradient Problem

**15. What is the mathematical formulation of the ReLU (Rectified Linear Unit) activation function?**
* A) $f(x) = \frac{1}{1 + e^{-x}}$
* B) $f(x) = \tanh(x)$
* C) $f(x) = \max(0, x)$
* D) $f(x) = \ln(1 + e^x)$

**16. Which activation function is almost exclusively used in the output layer of a neural network for multi-class classification problems?**
* A) Softmax
* B) Sigmoid
* C) ReLU
* D) Binary Step

**17. What is the precise output range of the standard Logistic Sigmoid activation function?**
* A) $[-1, 1]$
* B) $(0, 1)$
* C) $[0, \infty)$
* D) $(-\infty, \infty)$

**18. Why is Cross-Entropy Loss generally preferred over Mean Squared Error (MSE) for classification tasks in neural networks?**
* A) MSE cannot handle categorical input features.
* B) MSE is computationally impossible to differentiate.
* C) Cross-Entropy heavily penalizes confident but incorrect predictions and pairs perfectly with Sigmoid/Softmax to avoid learning slowdowns.
* D) Cross-Entropy automatically determines the correct number of hidden layers.

**19. Which regularization technique actively combats overfitting by randomly disabling a percentage of neurons in a layer during each forward pass of training?**
* A) Batch Normalization
* B) L2 Ridge Regularization
* C) Early Stopping
* D) Dropout

**20. Why is it a standard practice NOT to initialize all weights in an Artificial Neural Network to exactly zero?**
* A) Zero weights cause the backpropagation algorithm to divide by zero.
* B) It causes the "symmetry problem," where all neurons in a layer learn the exact same features and update identically, effectively reducing the network to a single neuron.
* C) It forces the activation functions to output negative infinity.
* D) Zero initialization is actually the preferred method for deep networks.

**21. In an artificial neuron, if the net input $z = \sum w_i x_i + b$ is calculated to be 0, what is the output if the activation function is a standard Sigmoid?**
* A) 0.5
* B) 0.0
* C) 1.0
* D) -1.0

**22. Which of the following best describes the "Feedforward" phase of a neural network?**
* A) Updating the weights based on the calculated error.
* B) Randomly initializing the biases before training begins.
* C) Passing input data through the network layer by layer to generate a prediction.
* D) Expanding the dataset using data augmentation techniques.

**23. The XOR problem requires a network with at least one hidden layer. How many total neurons (excluding the input nodes) are minimally required to solve XOR using binary step functions?**
* A) 1
* B) 2
* C) 4
* D) 3 (Two in the hidden layer, one in the output layer)

**24. What is the primary function of an Activation Function in an Artificial Neural Network?**
* A) To automatically normalize the input data.
* B) To introduce non-linearity into the model, allowing it to learn complex, non-planar decision boundaries.
* C) To calculate the final loss/error of the prediction.
* D) To define the learning rate for the gradient descent optimizer.

**25. If a hidden layer uses a linear activation function $f(x) = cx$, what is the mathematical consequence for a multi-layer network?**
* A) The entire network collapses into a single linear mathematical transformation, rendering the hidden layers entirely useless.
* B) The network will train infinitely faster with no loss of accuracy.
* C) The network automatically acts as a Random Forest.
* D) The gradient descent algorithm will fail to converge.

**26. Which metric fundamentally drives the Backpropagation algorithm by indicating how much each weight contributed to the final error?**
* A) The variance of the weights.
* B) The R-squared value of the output.
* C) The partial derivative of the Loss function with respect to each weight.
* D) The absolute magnitude of the bias term.

**27. In Mini-batch Gradient Descent, what dictates the number of samples processed before the model's internal parameters are updated?**
* A) The Epoch limit
* B) The Batch Size
* C) The Learning Rate
* D) The number of Hidden Layers

**28. Which of the following is an advantage of using the ReLU activation function over Sigmoid in deep networks?**
* A) ReLU outputs are strictly bounded between -1 and 1.
* B) ReLU prevents the network from overfitting completely.
* C) ReLU is perfectly differentiable at $x = 0$.
* D) ReLU does not suffer from the vanishing gradient problem in the positive domain.

**29. The term "Universal Approximation Theorem" states that a feedforward network can approximate any continuous function given:**
* A) At least one hidden layer with a sufficient number of non-linear neurons.
* B) A completely linear architecture with infinite depth.
* C) Strictly binary input values.
* D) An exact zero-error initialization.

**30. What problem does the "Dying ReLU" phenomenon describe?**
* A) Neurons activating too frequently, causing exploding gradients.
* B) The ReLU function turning into a Sigmoid function over time.
* C) Neurons getting stuck outputting zero for all inputs, resulting in a zero gradient and preventing any further weight updates.
* D) The network spontaneously dropping entire hidden layers.

**31. When a Perceptron updates its weights according to the learning rule $\Delta w_i = \eta (y - \hat{y}) x_i$, what happens if the prediction is correct ($y = \hat{y}$)?**
* A) The weight is halved.
* B) The weight is not updated ($\Delta w_i = 0$).
* C) The learning rate is increased.
* D) The bias is inverted.

**32. What is the primary difference between a Perceptron and an ordinary Logistic Regression model?**
* A) Logistic regression uses a hard step function; Perceptrons use a continuous sigmoid function.
* B) Perceptrons cannot use numerical inputs.
* C) Perceptrons only support unsupervised learning.
* D) A classic Perceptron uses a hard threshold (step function) for classification, whereas Logistic Regression outputs a continuous probability via the sigmoid function.

**33. What is the defining characteristic of a "Fully Connected" (Dense) layer in a Neural Network?**
* A) Every neuron in the layer is connected to every neuron in the subsequent layer.
* B) The layer has no activation function.
* C) The layer connects directly back to the input data.
* D) The weights are completely static and do not update.

**34. During Backpropagation, the gradient is passed backwards from the output layer to the hidden layers. Why is it called "Backpropagation of Error"?**
* A) Because the input data is fed backwards.
* B) Because it reverses the sign of all weights.
* C) Because it calculates the gradient of the error function by propagating the error derivative backwards through the network layers.
* D) Because the activation functions are inverted.

**35. If you are building a Neural Network for a Binary Classification task, what is the most appropriate configuration for the Output Layer?**
* A) 2 neurons with a Softmax activation.
* B) 1 neuron with a Sigmoid activation.
* C) 1 neuron with a ReLU activation.
* D) 2 neurons with a Linear activation.

**36. In multi-class classification, why does the Softmax function ensure the outputs can be interpreted as probabilities?**
* A) It converts all outputs to binary integers.
* B) It scales the outputs to match the variance of the inputs.
* C) It maps negative numbers to exactly zero.
* D) It exponentially scales the raw logits and normalizes them so the sum of all output node values equals exactly 1.

**37. Which optimization algorithm adapts the learning rate for each individual parameter, preventing slow convergence on sparse features?**
* A) Adam (Adaptive Moment Estimation)
* B) Standard Batch Gradient Descent
* C) The Normal Equation
* D) The Perceptron Learning Rule

**38. What is the derivative of the standard Sigmoid function $f(x)$ with respect to $x$?**
* A) $f(x) + (1 - f(x))$
* B) $1 - f(x)$
* C) $f(x) \cdot (1 - f(x))$
* D) $x \cdot f(x)$

**39. When modeling a NAND gate using a perceptron with inputs $x_1, x_2$, which combination of weights and bias would work (assuming a step function that fires if sum $\ge 0$)?**
* A) $w_1 = 1, w_2 = 1, $b=-1.$5$
* B) $w_1 = -1, w_2 = -1, $b=1.$5$
* C) $w_1 = 0, w_2 = 0, b = 0$
* D) $w_1 = 1, w_2 = -1, $b=-0.$5$

**40. What architectural structure allows Neural Networks to maintain a "memory" of previous inputs in a sequence, differentiating them from standard Feedforward networks?**
* A) Convolutional layers
* B) Max Pooling layers
* C) Dropout layers
* D) Recurrent connections (Recurrent Neural Networks - RNNs)

**41. In the context of loss landscapes, what is a "Local Minimum"?**
* A) A point where the loss is zero.
* B) The highest possible error the network can achieve.
* C) A point where the loss is lower than all immediately surrounding points, but not the lowest possible point in the entire parameter space.
* D) The exact optimal configuration of weights.

**42. Which of the following statements about the Hyperbolic Tangent (Tanh) activation function is true compared to the Sigmoid function?**
* A) Tanh is zero-centered (output ranges from -1 to 1), making optimization easier by preventing strongly skewed weight updates.
* B) Tanh is completely immune to the vanishing gradient problem.
* C) Tanh outputs only positive values.
* D) Tanh cannot be used in hidden layers.

**43. If a dataset has 10,000 samples and you use a Batch Size of 100, how many "Iterations" (weight updates) occur per Epoch?**
* A) 10
* B) 1,000
* C) 100,000
* D) 100

**44. What defines "Overfitting" in the context of training a Neural Network?**
* A) The network achieves low training error and low validation error.
* B) The network achieves very low training error but significantly higher validation error, failing to generalize.
* C) The network fails to learn the training data, resulting in high training error.
* D) The network trains too quickly and stops prematurely.

**45. What is the fundamental mechanism of "Early Stopping"?**
* A) Initializing the weights to zero to skip the first epoch.
* B) Stopping the gradient descent algorithm before the first epoch completes.
* C) Monitoring the validation loss during training and halting the training process when the validation loss stops decreasing or begins to increase.
* D) Pruning the neural network's hidden layers dynamically.

**46. Which matrix dimension rule must be satisfied to multiply an input vector $X$ (shape 1xM) by a Weight matrix $W$ in a fully connected layer?**
* A) The weight matrix $W$ must have exactly M rows (shape MxN).
* B) The weight matrix $W$ must be a perfect square.
* C) The input vector $X$ must be transposed to Nx1.
* D) The weight matrix must have a shape of 1xM.

**47. In the formula for a neural node $z = WX + b$, what mathematical entity does $W$ typically represent in a multi-neuron layer?**
* A) A scalar value
* B) A one-dimensional bias array
* C) A loss function derivative
* D) A two-dimensional weight matrix

**48. Why is Frank Rosenblatt significant in the history of Artificial Neural Networks?**
* A) He mathematically proved the limitations of single-layer networks.
* B) He invented the original Perceptron algorithm and built the hardware implementation (Mark I Perceptron).
* C) He derived the backpropagation algorithm.
* D) He invented the ReLU activation function.

**49. If a Neural Network has 3 input nodes, one hidden layer with 4 nodes, and an output layer with 2 nodes, how many weight parameters exist in the network? (Ignore biases)**
* A) 9
* B) 14
* C) 20
* D) 24

**50. What is "Stochastic Gradient Descent" (SGD)?**
* A) Updating the model's weights using exactly one randomly selected training sample per iteration.
* B) Updating the weights using the entire dataset simultaneously.
* C) Using a random learning rate for every epoch.
* D) Randomly deleting neurons during training.

**51. What does the concept of "Credit Assignment" refer to in Neural Networks?**
* A) Assigning memory resources to the GPU.
* B) Providing a bias term to the input layer.
* C) Determining the final classification probability.
* D) Determining how much each individual weight in the network contributed to the final error at the output layer.

**52. The function $f(x) = \frac{e^x - e^{-x}}{e^x + e^{-x}}$ is the mathematical definition of which activation function?**
* A) Sigmoid
* B) Tanh (Hyperbolic Tangent)
* C) ReLU
* D) Softplus

**53. Why does the "Vanishing Gradient" problem severely affect deep networks utilizing the Sigmoid function?**
* A) The derivative of the Sigmoid function reaches a maximum of only 0.25; multiplying these small derivatives across many layers shrinks the gradient exponentially toward zero.
* B) The Sigmoid function outputs negative values, reversing the gradient direction.
* C) Sigmoid requires binary inputs, which deep networks cannot provide.
* D) The gradient naturally explodes to infinity.

**54. In the context of Neural Networks, what is a "Hyperparameter"?**
* A) A parameter learned automatically by the network during training (e.g., weights).
* B) The predicted output value.
* C) A configuration setting defined prior to training that governs the learning process itself (e.g., Learning Rate, Batch Size, Architecture).
* D) The mathematical limit of the activation function.

**55. Which of the following architectures is explicitly designed to process spatial grid data, such as images?**
* A) Feedforward Neural Networks (FNN)
* B) Recurrent Neural Networks (RNN)
* C) Long Short-Term Memory networks (LSTM)
* D) Convolutional Neural Networks (CNN)

**56. When implementing an artificial neural network from scratch, why do we use a "Validation Set"?**
* A) To artificially expand the training data.
* B) To tune hyperparameters and monitor for overfitting independently of the final Test Set.
* C) To calculate the final, unbiased accuracy of the model for production.
* D) To initialize the weights optimally.

**57. If a perceptron outputs 1 when the weighted sum is $\ge \theta$ and 0 otherwise, what is the mathematically equivalent formulation using a bias $b$?**
* A) The perceptron outputs 1 when the weighted sum plus bias $b \ge 0$, where $b = -\theta$.
* B) The bias is ignored.
* C) The perceptron outputs 1 when $b \ge \theta$.
* D) The bias is equal to exactly $\theta / 2$.

**58. How did the Backpropagation algorithm resolve the structural limitation exposed by Minsky and Papert?**
* A) By proving XOR is linearly separable.
* B) By eliminating hidden layers.
* C) By providing a mathematically sound method to train the weights of hidden layers, allowing networks to learn complex, non-linear representations.
* D) By replacing artificial neurons with decision trees.

**59. In gradient descent, the mathematical formula to update a weight $w$ is $w_{new} = w_{old} - \eta \cdot \nabla J(w)$. What does $\nabla J(w)$ represent?**
* A) The learning rate.
* B) The previous weight value.
* C) The bias term.
* D) The gradient (derivative) of the cost/loss function with respect to the weight $w$.

**60. What is a "Leaky ReLU" activation function designed to fix?**
* A) The exploding gradient problem.
* B) The "Dying ReLU" problem, by allowing a small, non-zero gradient when $x < 0$ (e.g., $f(x) = 0.01x$).
* C) The computational cost of the exponential function.
* D) Overfitting in the output layer.

**61. Which of the following correctly pairs the Logic Gate with its linear separability status?**
* A) AND = Non-linearly separable
* B) OR = Non-linearly separable
* C) XOR = Non-linearly separable
* D) NAND = Non-linearly separable

**62. Which deep learning term describes the technique of normalizing the inputs of each layer to have a mean of 0 and a variance of 1 during training?**
* A) Batch Normalization
* B) Max Pooling
* C) Data Augmentation
* D) Xavier Initialization

**63. If a network is predicting a continuous numerical value (e.g., predicting house prices), what is the most appropriate activation function for the single output neuron?**
* A) Softmax
* B) Sigmoid
* C) Tanh
* D) Linear (or Identity)

**64. What defines a "Deep" Neural Network?**
* A) A network with extremely large weight values.
* B) A network comprising multiple hidden layers between the input and output layers.
* C) A network that only uses deep tree architectures.
* D) A network with more than 10,000 input features.

**65. In the context of building logic gates, what weights and bias threshold ($\theta$) could implement a standard AND gate for inputs $x_1, x_2 \in \{0,1\}$?**
* A) $w_1=1, w_2=1, \$theta=0.$5$
* B) $w_1=0.5, w_2=0.5, \$theta=0.$1$
* C) $w_1=1, w_2=1, \$theta=1.$5$
* D) $w_1=-1, w_2=-1, \$theta=-1.$5$

**66. What is the fundamental reason Neural Networks are often referred to as "Black Box" models?**
* A) Because their internal, highly dimensional matrix transformations and weighted representations are exceptionally difficult for humans to intuitively interpret or explain.
* B) Because they can only be programmed in hidden, compiled languages.
* C) Because their physical hardware enclosures are light-proof.
* D) Because the code for backpropagation is kept proprietary by major corporations.

**67. What does the "Momentum" hyperparameter essentially do in gradient descent algorithms?**
* A) It stops training when the loss is zero.
* B) It randomizes the weights after every epoch.
* C) It forces the learning rate to decay exponentially.
* D) It accelerates gradient descent by adding a fraction of the previous update vector to the current update, helping push past local minima and reducing oscillation.

**68. If the training accuracy is 99% but the testing accuracy is 60%, the neural network is severely:**
* A) Underfitting
* B) Overfitting
* C) Converging perfectly
* D) Experiencing Vanishing Gradients

**69. Which is true about the input layer of a Neural Network?**
* A) It applies the ReLU activation function to all features.
* B) It updates its weights via backpropagation.
* C) It performs no computation; it strictly holds and passes the input feature values to the first hidden layer.
* D) It always consists of a single neuron.

**70. The "Loss Function" in a neural network measures:**
* A) The disparity (error) between the model's predicted outputs and the actual target ground-truth values.
* B) The total execution time of the network.
* C) The amount of computer memory lost during matrix inversion.
* D) The ratio of hidden layers to input layers.

**71. Why is the Sigmoid activation function highly problematic for the hidden layers of very deep networks?**
* A) It forces all outputs to be negative.
* B) The derivatives in the "tails" of the function are extremely close to zero, causing gradients to vanish during backpropagation.
* C) It requires the inputs to be strictly integers.
* D) It causes the gradient to explode to infinity.

**72. What is "Weight Initialization" so critical in Deep Learning?**
* A) Because weights must be initialized to the final answer.
* B) Because it determines the number of layers in the network.
* C) Because it prevents the network from needing an activation function.
* D) Because poor initialization can immediately stall the network via vanishing/exploding gradients before learning even begins.

**73. In terms of neural networks, what does "MSE" stand for?**
* A) Matrix Sum Error
* B) Maximum Standard Exponent
* C) Mean Squared Error
* D) Minimum Scaled Entropy

**74. What is the difference between a Parameter and an Output in a Neural Network?**
* A) Parameters (weights/biases) are internal variables the model learns; Outputs are the final predictions generated by the network.
* B) Outputs are internal variables; Parameters are the final predictions.
* C) There is no difference.
* D) Parameters belong strictly to the input layer.

**75. The threshold logic unit (TLU), an early form of an artificial neuron, was introduced by:**
* A) Geoffrey Hinton
* B) Warren McCulloch and Walter Pitts
* C) Alan Turing
* D) Andrew Ng

**76. Which of the following represents a non-convex optimization space?**
* A) A bowl-shaped surface with a single global minimum.
* B) A perfectly flat surface.
* C) A straight line.
* D) A rugged landscape with multiple peaks, valleys, local minima, and saddle points.

**77. How does L2 Regularization (Ridge) alter the cost function to prevent overfitting?**
* A) It subtracts the learning rate from the error.
* B) It takes the square root of the predictions.
* C) It adds a penalty term proportional to the square of the magnitude of the weights.
* D) It randomly deletes input features.

**78. The output of a Softmax activation function applied to a layer of 5 neurons will always:**
* A) Sum exactly to 1.0.
* B) Be exactly 0 or 1 for every neuron.
* C) Sum exactly to 0.
* D) Match the variance of the input vector.

**79. What defines a "Feedforward" neural network?**
* A) Information flows backwards through the network continuously.
* B) Information flows in only one direction, from input to output, with no loops or cycles.
* C) The network randomly feeds data forward and backward.
* D) The network has no hidden layers.

**80. In a multi-class neural network classifier, if the ground truth is Class 2 (out of classes 0, 1, 2), what does the One-Hot Encoded target vector look like?**
* A) `[0, 1, 0]`
* B) `[2]`
* C) `[0.33, 0.33, 0.33]`
* D) `[0, 0, 1]`

**81. Which loss function is mathematically paired with the Softmax output for classification?**
* A) Categorical Cross-Entropy Loss
* B) Mean Absolute Error (MAE)
* C) Mean Squared Error (MSE)
* D) Huber Loss

**82. Why do Neural Networks require numerical data?**
* A) Because text data causes the computer to crash.
* B) Because categorical variables cannot be classified.
* C) Because the entire network is built on mathematical operations (matrix multiplication, weighted sums, derivatives) that fundamentally require numerical inputs.
* D) Because numerical data requires less RAM.

**83. If a 10-layer neural network is suffering from the Vanishing Gradient problem, how will the weights behave during training?**
* A) The weights in the layers closest to the output will stop updating, while the input layers update rapidly.
* B) The weights in the layers closest to the input will update extremely slowly or not at all, severely stalling learning.
* C) All weights will update uniformly to exactly zero.
* D) All weights will explode to infinity.

**84. The function $f(x) = \max(0.01x, x)$ represents which activation function?**
* A) Sigmoid
* B) Softmax
* C) Standard ReLU
* D) Leaky ReLU

**85. What is the fundamental difference between the Delta Rule (Gradient Descent) and the Perceptron Learning Rule?**
* A) The Delta Rule updates weights using the continuous derivative of the error function, while the Perceptron Rule updates based on a binary step threshold error.
* B) The Perceptron Rule requires complex calculus.
* C) The Delta Rule can only be applied to single-layer networks.
* D) There is no mathematical difference.

**86. A hidden layer contains 100 neurons. If Dropout is applied to this layer with a rate of 0.2 during training, what happens?**
* A) All weights are multiplied by 0.2.
* B) The learning rate for this layer is set to 0.2.
* C) On each forward pass, 20 randomly selected neurons in this layer are temporarily deactivated (outputs set to 0).
* D) 20 neurons are permanently deleted from the network.

**87. In matrix operations for a neural network layer, $Z = X \cdot W + b$. If $X$ has shape (batch_size, input_features) and $W$ has shape (input_features, output_nodes), what is the shape of $Z$?**
* A) (input_features, batch_size)
* B) (batch_size, output_nodes)
* C) (output_nodes, input_features)
* D) (batch_size, input_features)

**88. Which optimization technique seeks to normalize the inputs to a layer across the mini-batch to stabilize and accelerate training?**
* A) Dropout
* B) L1 Regularization
* C) Weight Decay
* D) Batch Normalization

**89. To solve the XOR problem, a neural network creates non-linear decision boundaries. How is this achieved visually in the feature space?**
* A) By warping the feature space so the previously non-linearly separable points can be separated by a straight hyperplane in a higher dimension.
* B) By drawing a single straight line through the origin.
* C) By converting the 2D problem into a 1D problem.
* D) By deleting one of the conflicting data points.

**90. Which of the following correctly describes "Fine-tuning" in the context of neural networks?**
* A) Deleting all the weights and starting over.
* B) Building a network with exclusively 1 neuron.
* C) Taking a pre-trained network and slightly adjusting its weights by training it on a new, specific dataset, often with a very small learning rate.
* D) Adjusting the physical hardware of the computer to run faster.

**91. What is the derivative of the ReLU activation function $f(x) = \max(0, x)$ when $x > 0$?**
* A) $x$
* B) $0$
* C) $-1$
* D) $1$

**92. Why does the "Exploding Gradient" problem cause training to fail?**
* A) It causes the loss function to reach zero prematurely.
* B) Massive error gradients accumulate, resulting in extremely large updates to neural network weights, making the model unstable and often resulting in `NaN` numerical overflows.
* C) It forces the network to drop all hidden layers.
* D) It causes the network to perfectly memorize the data.

**93. What is the fundamental concept behind a "Saddle Point" in the loss landscape of a neural network?**
* A) A point where the gradient is zero, but it is a minimum along one axis and a maximum along another, often stalling standard gradient descent algorithms.
* B) The absolute lowest point of the loss function.
* C) A point where the learning rate naturally doubles.
* D) A point that automatically triggers dropout.

**94. In backpropagation, the error term for an output node $j$ with a Sigmoid activation function relies on the derivative $o_j(1 - o_j)$. Where does this expression come from?**
* A) The Pythagorean theorem.
* B) The derivative of the Mean Squared Error.
* C) The mathematical derivative of the Sigmoid activation function itself.
* D) The inverse of the learning rate.

**95. A neural network architecture where the output of one layer is passed back as the input to a previous layer is fundamentally a:**
* A) Feedforward Network
* B) Radial Basis Function Network
* C) Support Vector Machine
* D) Recurrent Neural Network

**96. What is "Data Augmentation" typically used for when training deep neural networks?**
* A) To reduce the amount of RAM needed.
* B) To artificially increase the size and diversity of the training dataset (e.g., rotating or flipping images) to help the model generalize and prevent overfitting.
* C) To bypass the backpropagation algorithm.
* D) To increase the learning rate automatically.

**97. Which formula accurately represents the calculation of a single weight update in standard Gradient Descent? ($w$ is weight, $\alpha$ is learning rate, $J$ is cost)**
* A) $w = w - \alpha \frac{\partial J}{\partial w}$
* B) $w = w + \alpha \frac{\partial J}{\partial w}$
* C) $w = \alpha - w \frac{\partial J}{\partial w}$
* D) $w = \frac{\partial J}{\partial w} - \alpha$

**98. The number of parameters in a deep neural network often exceeds the number of training examples. What is this phenomenon highly susceptible to?**
* A) Underfitting
* B) Perfect linear separation
* C) Severe Overfitting
* D) The symmetry problem

**99. Which of the following is an example of an unsupervised Neural Network architecture?**
* A) Standard Multi-Layer Perceptron
* B) Convolutional Neural Network classifying cats vs dogs
* C) Recurrent Neural Network translating English to French
* D) Autoencoder

**100. In a typical Multi-Layer Perceptron, what determines the dimensionality (size) of the final Output Layer?**
* A) The specific task requirement (e.g., 1 node for binary classification/regression, $N$ nodes for $N$-class classification).
* B) It must always be exactly equal to the dimensionality of the Input Layer.
* C) It must be a power of 2.
* D) It is determined randomly by the optimizer.

---

### **Answers: Questions 1 – 50**

| | | | | | | | | | |
|---|---|---|---|---|---|---|---|---|---|
| **1.** B | **2.** B | **3.** C | **4.** B | **5.** D | **6.** A | **7.** C | **8.** B | **9.** D | **10.** A |
| **11.** C | **12.** A | **13.** B | **14.** D | **15.** C | **16.** A | **17.** B | **18.** C | **19.** D | **20.** B |
| **21.** A | **22.** C | **23.** D | **24.** B | **25.** A | **26.** C | **27.** B | **28.** D | **29.** A | **30.** C |
| **31.** B | **32.** D | **33.** A | **34.** C | **35.** B | **36.** D | **37.** A | **38.** C | **39.** B | **40.** D |
| **41.** C | **42.** A | **43.** D | **44.** B | **45.** C | **46.** A | **47.** D | **48.** B | **49.** C | **50.** A |

<br>

### **Answers: Questions 51 – 100**

| | | | | | | | | | |
|---|---|---|---|---|---|---|---|---|---|
| **51.** D | **52.** B | **53.** A | **54.** C | **55.** D | **56.** B | **57.** A | **58.** C | **59.** D | **60.** B |
| **61.** C | **62.** A | **63.** D | **64.** B | **65.** C | **66.** A | **67.** D | **68.** B | **69.** C | **70.** A |
| **71.** B | **72.** D | **73.** C | **74.** A | **75.** B | **76.** D | **77.** C | **78.** A | **79.** B | **80.** D |
| **81.** A | **82.** C | **83.** B | **84.** D | **85.** A | **86.** C | **87.** B | **88.** D | **89.** A | **90.** C |
| **91.** D | **92.** B | **93.** A | **94.** C | **95.** D | **96.** B | **97.** A | **98.** C | **99.** D | **100.** A |