# Chapter 3: Decision Tree - Advanced MCQ Assessment

**1. What structural function does a "Leaf Node" (or Terminal Node) serve in a standard classification tree?**
* A) It evaluates a specific attribute against a threshold to branch the data.
* B) It represents the entire population or sample before any splitting occurs.
* C) It assigns the final class label or outcome and does not split the data further.
* D) It acts as an intermediate point where data is purged of outliers.

**2. Why are Decision Trees mathematically classified as "non-parametric" models?**
* A) Because they cannot process continuous numerical parameters.
* B) Because they do not make any strong underlying assumptions about the spatial distribution of the data.
* C) Because they require hyperparameters to be explicitly defined before training.
* D) Because they rely exclusively on Euclidean distance metrics rather than parametric equations.

**3. Which of the following decision tree algorithms is specifically noted for utilizing the Chi-square statistical test to determine the optimal multi-way split?**
* A) ID3
* B) CART
* C) C4.5
* D) CHAID

**4. In the context of decision tree induction, what is the fundamental limitation of the "greedy" approach used by most standard algorithms?**
* A) It takes exponentially longer to train than non-greedy approaches.
* B) It makes locally optimal choices at each node, which does not guarantee the discovery of the globally optimal tree structure.
* C) It systematically ignores continuous variables in favor of categorical ones.
* D) It forces the tree to overfit by generating too many leaf nodes per split.

**5. How does the CART (Classification and Regression Trees) algorithm structurally differentiate itself from the C4.5 algorithm?**
* A) CART only supports classification tasks, whereas C4.5 supports both classification and regression.
* B) CART relies on the Information Gain ratio, whereas C4.5 relies on the Gini Index.
* C) CART strictly constructs binary trees (two branches per internal node), whereas C4.5 can construct multi-way splits.
* D) CART cannot handle missing values, while C4.5 utilizes mean imputation inherently.

**6. For a binary classification problem, what is the mathematical maximum value of Entropy, and when does it occur?**
* A) 0.5, occurring when the node is perfectly pure.
* B) 1.0, occurring when the classes are perfectly balanced (e.g., 50% positive, 50% negative).
* C) 1.0, occurring when the node contains only one class.
* D) 2.0, occurring when there are exactly two attributes to split on.

**7. If a dataset $S$ contains 14 instances, with 9 "Yes" and 5 "No" outcomes, which expression correctly represents its initial Entropy?**
* A) $1 - ( (\frac{9}{14})^2 + (\frac{5}{14})^2 )$
* B) $-\frac{9}{14}\log_2(\frac{9}{14}) - \frac{5}{14}\log_2(\frac{5}{14})$
* C) $\frac{9}{14}\log_2(\frac{5}{14}) + \frac{5}{14}\log_2(\frac{9}{14})$
* D) $-\frac{14}{9}\log_2(\frac{14}{9}) - \frac{14}{5}\log_2(\frac{14}{5})$

**8. What specific structural bias in the standard Information Gain metric does the "Gain Ratio" attempt to correct?**
* A) Its bias against attributes with high variance.
* B) Its inability to process continuous numerical targets.
* C) Its inherent bias toward selecting attributes with a very large number of distinct values (e.g., ID columns).
* D) Its tendency to create perfectly balanced, symmetrical binary trees.

**9. Which of the following best describes the core mechanism of "Pre-pruning" (early stopping) in decision trees?**
* A) Allowing the tree to grow to its maximum depth, then retroactively removing branches that do not improve cross-validation accuracy.
* B) Converting categorical variables into numerical values before the tree algorithm begins splitting.
* C) Halting the growth of the tree dynamically during the training phase based on predefined conditions like `max_depth` or `min_samples_split`.
* D) Using an ensemble of multiple trees to vote on the most important features to prune.

**10. "Cost-complexity pruning" is a statistical technique most strongly associated with which phase of decision tree optimization?**
* A) Data preprocessing and encoding.
* B) Post-pruning (pruning an already fully-grown tree).
* C) Pre-pruning (halting growth before it finishes).
* D) Hyperparameter grid searching.

**11. Which attribute selection measure defines "Impurity" as the probability of a randomly chosen element being incorrectly classified if it was randomly labeled according to the distribution of labels in the subset?**
* A) Gain Ratio
* B) Information Gain
* C) Entropy
* D) Gini Index

**12. In Python's `scikit-learn`, what is the default criterion utilized by the `DecisionTreeClassifier` to evaluate the quality of a split?**
* A) 'entropy'
* B) 'log_loss'
* C) 'gini'
* D) 'variance'

**13. In the canonical "Play Tennis" dataset example provided in the materials, what is the total number of instances (days) used to calculate the root entropy?**
* A) 10
* B) 12
* C) 14
* D) 16

**14. What does the term `min_samples_leaf` control when instantiating a `DecisionTreeClassifier` in Scikit-Learn?**
* A) The minimum number of samples required to split an internal decision node.
* B) The minimum number of samples that must be present in a newly created leaf node for the split to be considered valid.
* C) The absolute minimum depth the tree must reach before stopping.
* D) The minimum number of features to consider when looking for the best split.

**15. Random Forest mitigates the high variance and overfitting tendencies of a single decision tree by utilizing which overarching machine learning paradigm?**
* A) Dimensionality Reduction
* B) Unsupervised Clustering
* C) Ensemble Learning (specifically Bagging)
* D) Gradient Boosting

**16. Which of the following is a primary DISADVANTAGE of a standard Decision Tree model?**
* A) It requires extensive data scaling and normalization before training.
* B) It is highly unstable; small variations in the training data can result in a completely different tree structure being generated.
* C) It cannot handle datasets that contain a mixture of numerical and categorical data.
* D) Its internal decision-making process is a "black box" that cannot be interpreted by humans.

**17. The ID3 decision tree algorithm was originally developed by which prominent computer scientist?**
* A) Andrew Ng
* B) Yann LeCun
* C) Ross Quinlan
* D) Leo Breiman

**18. Which mathematical operation is strictly used to compute the Information Gain of an attribute $A$ relative to a dataset $S$?**
* A) Adding the Entropy of $S$ to the expected Entropy of $S$ after splitting by $A$.
* B) Multiplying the Entropy of $S$ by the Gini index of $A$.
* C) Subtracting the expected (weighted) Entropy of $S$ after splitting by $A$ from the original Entropy of $S$.
* D) Dividing the Entropy of $S$ by the Intrinsic Information of $A$.

**19. When classifying an "Animal" dataset based on the attributes 'Color' and 'Size', what happens if two identical instances in the training data possess the exact same attributes but different target class labels?**
* A) The tree will infinitely loop and fail to converge.
* B) The algorithm will average the labels and output a continuous regression value.
* C) The resulting leaf node will remain impure, and the majority class will typically dictate the prediction.
* D) The algorithm will automatically drop the conflicting rows during the preprocessing phase.

**20. What does the `n_estimators` hyperparameter dictate in a Scikit-Learn `RandomForestClassifier`?**
* A) The maximum depth of each individual tree in the forest.
* B) The number of decision trees to generate in the ensemble.
* C) The specific mathematical criterion used for splitting (Gini vs. Entropy).
* D) The number of parallel processing jobs to run during training.

**21. According to the tutorial, what is the primary purpose of the `LabelEncoder` class from `sklearn.preprocessing`?**
* A) To automatically impute missing target variables using the mode.
* B) To convert continuous target variables into discrete bins.
* C) To transform categorical text labels (e.g., 'Yes', 'No') into machine-readable integer values.
* D) To scale numerical features to a range between 0 and 1.

**22. If a decision tree is allowed to grow until every single leaf node contains exactly one training instance, what is the most likely consequence?**
* A) Severe Underfitting
* B) Severe Overfitting
* C) Perfect generalized testing accuracy
* D) A completely symmetric tree structure

**23. Which ensemble technique involves training multiple decision trees on random subsets of the data (sampled with replacement) to reduce variance?**
* A) Boosting
* B) Stacking
* C) Bagging (Bootstrap Aggregating)
* D) Voting

**24. In the Scikit-Learn `classification_report`, which metric explicitly calculates the ratio of correctly predicted positive observations to the total predicted positive observations ($TP / (TP + FP)$)?**
* A) Recall
* B) F1-Score
* C) Support
* D) Precision

**25. In the context of Random Forests, feature randomness implies that: **
* A) The target labels are randomly shuffled before training each tree.
* B) Each tree is trained on a completely random, disjoint subset of rows.
* C) At each node split, the algorithm only evaluates a random subset of the available features, rather than searching all features greedily.
* D) The hyperparameters of the trees are randomized using a Gaussian distribution.

**26. How does a Decision Tree typically handle continuous numerical attributes during the splitting process?**
* A) It converts them into categorical bins based on equal frequency before training.
* B) It sorts the continuous values and evaluates potential binary threshold splits (e.g., $Attribute < X$ vs $Attribute \ge X$) to find the threshold that maximizes information gain.
* C) It ignores continuous attributes unless specifically forced by hyperparameter tuning.
* D) It applies a one-hot encoding mechanism to every unique numerical value.

**27. What is the theoretical maximum value of the Gini Impurity for a node containing exactly two equally distributed classes?**
* A) 0.0
* B) 0.5
* C) 0.75
* D) 1.0

**28. Which algorithm is specifically an extension of ID3 that accounts for continuous variables and missing values?**
* A) C4.5
* B) CART
* C) CHAID
* D) MARS

**29. The `plot_tree` function imported from `sklearn.tree` is used for what primary purpose?**
* A) To trace the memory allocation of the tree algorithm.
* B) To visually render the hierarchical structure, thresholds, and impurities of the trained decision tree.
* C) To automatically prune the tree visually.
* D) To plot the gradient descent cost function against the number of epochs.

**30. What does a Gini Impurity of 0.0 mathematically signify for a specific node in a decision tree?**
* A) The node contains an equal number of samples from all classes.
* B) The node is perfectly pure; all samples in the node belong to exactly one single class.
* C) The algorithm failed to find a valid split threshold.
* D) The node has reached the maximum permitted depth.

**31. In the "Play Tennis" example, if the 'Outlook' attribute has three possible values (Sunny, Overcast, Rain), how many branches will an ID3 algorithm generate from the 'Outlook' node?**
* A) 1
* B) 2
* C) 3
* D) 4

**32. When evaluating a classification model, the F1-Score is best described as:**
* A) The arithmetic mean of Precision and Accuracy.
* B) The harmonic mean of Precision and Recall, providing a balance between the two metrics.
* C) The total number of true positives divided by the total number of samples.
* D) The exact identical calculation to the Gini Index.

**33. What is the primary difference between how standard Decision Trees and Random Forests handle feature selection at each node?**
* A) Decision Trees select from a random subset of features; Random Forests select from all available features.
* B) Decision Trees select from all available features; Random Forests select from a random subset of features.
* C) Decision Trees only use categorical features; Random Forests only use numerical features.
* D) There is no difference; both evaluate all features greedily at every node.

**34. Which of the following is a direct consequence of setting `$max_depth=1$` in a `DecisionTreeClassifier`?**
* A) The model will generate a "Decision Stump", consisting of exactly one root node and its immediate leaves.
* B) The model will infinitely recurse until memory is exhausted.
* C) The model will utilize all available features simultaneously.
* D) The tree will perfectly overfit the training data.

**35. If a Decision Tree splits a dataset of 20 samples perfectly into two leaf nodes (one with 10 'A' samples, one with 10 'B' samples), what is the weighted average entropy of these two children nodes?**
* A) 0.0
* B) 0.5
* C) 1.0
* D) 2.0

**36. In Scikit-Learn, what is the default behavior for the `random_state` parameter if it is left unspecified (i.e., `None`) during the instantiation of a Random Forest?**
* A) It defaults to an integer value of 42.
* B) It uses the global NumPy random number generator, resulting in potentially different random splits upon each execution.
* C) It forces the algorithm to act deterministically, never using randomness.
* D) It triggers a warning and aborts the training process.

**37. How does a Decision Tree inherently handle non-linear relationships between features and the target variable?**
* A) It cannot handle non-linear relationships and requires polynomial feature engineering prior to training.
* B) It naturally captures non-linearities by recursively partitioning the feature space into distinct rectangular regions based on simple threshold rules.
* C) It applies a Sigmoid activation function to the input data automatically.
* D) It forces all non-linear relationships into a linear regression line at the leaf nodes.

**38. Which of the following tree-based algorithms was originally designed exclusively for categorical attributes, making it mathematically impossible to use for continuous target regression without significant modification?**
* A) MARS
* B) CART
* C) ID3
* D) Random Forest

**39. When calculating the "Support" metric in a `classification_report`, what specific value is being reported?**
* A) The number of actual occurrences of a specific class in the specified dataset.
* B) The statistical confidence interval of the precision metric.
* C) The number of trees in the Random Forest that "supported" the final prediction.
* D) The weight assigned to the Gini index.

**40. What is the mathematical definition of Recall in a binary classification matrix?**
* A) $TP / (TP + FP)$
* B) $TP / (TP + FN)$
* C) $TN / (TN + FP)$
* D) $(TP + TN) / (Total)$

**41. In the tutorial's Python code, the expression `df.dropna(inplace=True)` executes which specific data preprocessing action?**
* A) It replaces all missing numerical values with the mean of their respective columns.
* B) It removes any rows (samples) from the DataFrame that contain at least one missing (NaN) value, modifying the original DataFrame directly.
* C) It permanently deletes columns (features) that contain mostly missing values.
* D) It drops the target variable column in preparation for unsupervised learning.

**42. Which of the following is NOT an advantage of Decision Trees mentioned in the course materials?**
* A) They are easy to understand and visualize.
* B) They require very little data preparation (e.g., no scaling required).
* C) They are robust to outliers in the feature space.
* D) They mathematically guarantee finding the globally optimal tree structure.

**43. A specific internal node $N$ has an Entropy of 0.8. It splits the data into two subsets, $N_1$ and $N_2$, which have an expected (weighted) Entropy of 0.3. What is the Information Gain of this split?**
* A) 1.1
* B) 0.5
* C) 0.24
* D) 2.66

**44. What defines a "Root Node" in the context of decision tree terminology?**
* A) It is the final node that provides the predicted probability.
* B) It represents the single attribute with the lowest Information Gain.
* C) It is the topmost node in the tree, representing the entire population or dataset before any partitions are made.
* D) It is an internal node that splits into exactly three branches.

**45. If the `min_samples_split` hyperparameter is set to 10, what will the algorithm do when evaluating a node containing 8 samples?**
* A) It will force a split regardless of impurity.
* B) It will immediately convert the node into a leaf node and assign the majority class label, refusing to split it further.
* C) It will borrow 2 samples from an adjacent node to meet the requirement.
* D) It will crash and throw a Python `ValueError`.

**46. Which algorithm name stands for "Multivariate Adaptive Regression Splines"?**
* A) CART
* B) C4.5
* C) MARS
* D) CHAID

**47. When using the `plot_tree` method, what visual indication shows that a node has a high degree of impurity (if `filled=True` is utilized)?**
* A) The node box is rendered with a perfectly transparent background.
* B) The node box is rendered with a dark, deeply saturated color.
* C) The node box is rendered with a light, mixed, or pale color, whereas pure nodes are deeply saturated with a class-specific color.
* D) The node is drawn with a dashed border.

**48. Why is "Accuracy" sometimes considered a misleading evaluation metric for classification tasks?**
* A) It cannot be calculated for multi-class problems.
* B) It can be highly skewed by severely imbalanced datasets, where predicting the majority class exclusively yields a high accuracy score while ignoring the minority class entirely.
* C) It is strictly mathematically identical to Precision.
* D) It relies on calculating the square root of the errors.

**49. In a Random Forest, what is the final classification prediction of the ensemble based upon?**
* A) The average of the Gini impurities across all trees.
* B) The prediction of the single tree with the deepest structure.
* C) A majority voting system aggregating the predictions of all the individual decision trees.
* D) A completely random selection from the pool of possible classes.

**50. What is the primary functional consequence of setting `max_depth` to a very large number (or `None`)?**
* A) The model enforces strict pre-pruning to maintain high bias.
* B) The tree is allowed to expand boundlessly until all leaves are pure or contain fewer than `min_samples_split` samples, significantly increasing the risk of overfitting.
* C) The algorithm automatically switches from Gini Impurity to Entropy calculation.
* D) The training data is subsampled heavily to prevent memory overflow.

**51. Which of the following is true regarding how a Decision Tree handles outliers in continuous feature attributes?**
* A) Decision Trees are highly sensitive to outliers because they use Mean Squared Error to determine classification splits.
* B) Decision trees are generally robust to outliers because they split based on ordered thresholds (e.g., $X > 100$) rather than calculating absolute distances or variances.
* C) Outliers cause the algorithm to permanently delete the affected rows during the initial root split.
* D) Outliers artificially force the tree to become perfectly symmetric.

**52. An attribute selection metric that calculates the ratio between Information Gain and the "Intrinsic Information" (or Split Info) of an attribute is known as:**
* A) Gini Index
* B) Gain Ratio
* C) Chi-Square Statistic
* D) MSE (Mean Squared Error)

**53. How does the `RandomForestClassifier` achieve "Bagging"?**
* A) By extracting only the categorical features and storing them in a "bag" array.
* B) By sequentially passing the errors of one tree to the next tree.
* C) By training each decision tree on a random sample of the training dataset drawn *with replacement*.
* D) By averaging the numerical values of the target variable.

**54. If a decision tree algorithm encounters a continuous numerical feature like "Temperature", how many potential split points must it theoretically evaluate to find the best split for that feature?**
* A) Exactly two split points: the mean and the median.
* B) Exactly one split point: the value that minimizes the Gini index for the entire dataset.
* C) It must sort the unique values of the feature and evaluate the midpoints between every adjacent pair of distinct values.
* D) It only evaluates splits at integers divisible by 10.

**55. What is the mathematical lower bound (minimum possible value) for Entropy?**
* A) -1.0
* B) 0.0
* C) 0.5
* D) 1.0

**56. In the tutorial example of predicting whether a person will buy a computer, if the dataset consists of features like Age, Income, and Credit Rating, what is the role of these features in the tree?**
* A) They represent the Leaf Nodes.
* B) They act as the Internal Decision Nodes where splitting criteria are applied.
* C) They represent the final classification output.
* D) They determine the random state of the model.

**57. If a branch in a decision tree leads to a subset of data where every single instance belongs to the class "Yes", what action will the algorithm take next for that specific branch?**
* A) It will calculate the Information Gain and split again.
* B) It will immediately prune the entire branch back to the root.
* C) It will declare the node a Leaf Node with the label "Yes" and halt further splitting on that branch, as the node is pure.
* D) It will randomly reassign 10% of the instances to class "No" to prevent overfitting.

**58. Which of the following scenarios describes the concept of "Underfitting" in a Decision Tree?**
* A) A tree that is so deep it captures the random noise of the training data perfectly.
* B) A tree with a `max_depth` of 1 applied to a highly complex, non-linear dataset, resulting in poor accuracy on both training and testing data.
* C) A Random Forest with 1000 estimators achieving 99% accuracy on the test set.
* D) A tree that has been completely post-pruned into a single root node.

**59. In the context of the CART algorithm, how are multi-class categorical variables handled during splitting?**
* A) By creating a multi-way split with a branch for every single category.
* B) By converting them to continuous variables using their alphabetical index.
* C) By evaluating all possible binary partitions (groupings) of the categories and selecting the one that minimizes the Gini impurity.
* D) CART strictly refuses to process multi-class categorical variables.

**60. The equation $H(S) = - \sum_{$i=1$}^{c} p_i \log_2 p_i$ calculates Entropy. What does the variable '$c$' represent in this formula?**
* A) The total number of instances in the dataset.
* B) The number of distinct features available for splitting.
* C) The number of distinct classes in the target variable.
* D) The mathematical constant representing the speed of light.

**61. A dataset has 20 samples: 15 belong to Class 0, and 5 belong to Class 1. What is the precise Gini Impurity of this dataset?**
* A) $1 - ((15/20)^2 + (5/20)^2)$ = 0.375
* B) $-(15/20)\log_2(15/20) - (5/20)\log_2(5/20)$ = 0.811
* C) $1 - (15/20) - (5/20)$ = 0.0
* D) $(15/20) \times (5/20)$ = 0.1875

**62. What does the term "Sub-tree" refer to in the topology of a decision tree?**
* A) The singular path from the root node to the first leaf node.
* B) An entire section of the tree consisting of an internal node and all its subsequent descendant nodes and leaves.
* C) The dataset remaining after all missing values have been dropped.
* D) A secondary, parallel decision tree built to check the errors of the primary tree.

**63. Which hyperparameter directly restricts the maximum number of levels a decision tree is allowed to grow downwards from the root?**
* A) `min_samples_split`
* B) `max_features`
* C) `max_depth`
* D) `min_weight_fraction_leaf`

**64. When interpreting a `classification_report`, what is the definition of "Macro Average" for a given metric like F1-score?**
* A) The metric is calculated globally by counting the total true positives, false negatives, and false positives across all classes.
* B) The metric is calculated for each class independently, and then the unweighted arithmetic mean of those scores is taken, treating all classes equally regardless of support.
* C) The metric is calculated by weighting the score of each class by its number of true instances (support).
* D) The maximum score achieved among all the distinct classes.

**65. Which of the following best describes the bias-variance tradeoff profile of an unpruned, fully grown Decision Tree?**
* A) High Bias, Low Variance
* B) Low Bias, High Variance
* C) High Bias, High Variance
* D) Low Bias, Low Variance

**66. What is the fundamental goal of the "Information Gain" metric when evaluating potential feature splits?**
* A) To maximize the resulting entropy of the child nodes.
* B) To find the split that maximizes the reduction in entropy (or impurity) from the parent node to the children nodes.
* C) To select the feature with the highest correlation to the primary key.
* D) To ensure the resulting tree remains perfectly balanced in depth.

**67. In Scikit-learn, what happens if you pass a Pandas DataFrame containing string data (like 'Sunny', 'Rain') directly into `DecisionTreeClassifier.fit()` without preprocessing?**
* A) The algorithm automatically applies One-Hot Encoding and proceeds normally.
* B) The algorithm automatically applies Label Encoding and proceeds normally.
* C) The algorithm throws an error because standard Scikit-Learn decision trees require all input features to be numerical.
* D) The algorithm silently drops those string columns and trains on the remaining numerical data.

**68. How does a Random Forest prediction handle a regression task (continuous numerical output) as opposed to a classification task?**
* A) It outputs the majority vote of the classification labels.
* B) It computes the mean (average) of the predictions from all the individual decision trees in the ensemble.
* C) It randomly selects one tree and uses its raw output.
* D) Random Forests are strictly incapable of performing regression tasks.

**69. Which term describes a decision tree algorithm where the construction happens in a top-down, recursive, divide-and-conquer manner?**
* A) Agglomerative Clustering
* B) Greedy Induction
* C) Stochastic Gradient Descent
* D) Backpropagation

**70. What is the primary motivation for converting categorical targets into numerical labels (e.g., using `LabelEncoder`) before evaluating accuracy with `accuracy_score` in Scikit-Learn?**
* A) `accuracy_score` inherently cannot process strings under any circumstances.
* B) While `accuracy_score` can often handle string arrays, converting to numerical arrays aligns data types with model outputs, preventing subtle type-mismatch errors during comparison.
* C) String targets cause the Random Forest algorithm to crash.
* D) Label Encoding strictly bounds the accuracy metric between 0 and 1.

**71. In a node with 10 instances of Class A and 10 instances of Class B, what is the value of the Gini Impurity?**
* A) $1 - (0.5^2 + 0.5^2) = 0.5$
* B) $1 - (1.0^2 + 0.0^2) = 0.0$
* C) $-(0.5\log_2 0.5) - (0.5\log_2 0.5) = 1.0$
* D) $0.5 \times 0.5 = 0.25$

**72. Why is Post-Pruning generally considered more robust (though more computationally expensive) than Pre-Pruning?**
* A) Post-pruning executes entirely in linear time $O(N)$.
* B) Pre-pruning suffers from the "horizon effect," where it might halt growth prematurely at a node that yields little immediate gain but would have led to highly significant splits further down.
* C) Post-pruning completely eliminates the need for calculating Information Gain.
* D) Pre-pruning requires the user to manually delete nodes via a graphical interface.

**73. In the context of decision trees, what does the "Play Tennis" dataset attempt to predict?**
* A) The specific temperature on a given day.
* B) The probability of rain occurring.
* C) Whether or not conditions are suitable to play tennis (Yes/No) based on weather attributes.
* D) The specific ID number of the tennis match.

**74. What is the primary difference between a classification tree and a regression tree?**
* A) Classification trees handle numerical targets; regression trees handle categorical targets.
* B) Classification trees predict discrete categorical class labels; regression trees predict continuous numerical values.
* C) Classification trees use the ID3 algorithm exclusively; regression trees use the C4.5 algorithm exclusively.
* D) There is no mathematical difference between the two structures.

**75. What does the term "Bagging" stand for in the context of Random Forests?**
* A) Binary Aggregation
* B) Basic Algorithmic Grouping
* C) Bootstrap Aggregating
* D) Backpropagated Gradient Generation

**76. If you have a highly imbalanced dataset (e.g., 99% Class A, 1% Class B), why might a basic Decision Tree struggle to accurately classify Class B?**
* A) Because the Gini index cannot calculate probabilities less than 5%.
* B) Because the algorithm will greedily favor splits that maximize overall purity, often resulting in trees that prioritize the massive majority class and ignore the minority.
* C) Because the maximum depth will be artificially limited to 1.
* D) Because the Entropy of an imbalanced dataset is always mathematically undefined.

**77. In the Scikit-Learn instantiation `RandomForestClassifier($n_estimators=100$, $random_state=42$)`, what is the effect of setting `$random_state=42$`?**
* A) It sets the initial node impurity to exactly 0.42.
* B) It forces the algorithm to use exactly 42 distinct features per split.
* C) It ensures the random sampling of data and features is deterministic, yielding exactly reproducible results across multiple runs.
* D) It limits the maximum depth of the trees to 42 levels.

**78. Which statistical method does the CHAID algorithm utilize to evaluate the significance of a split and act as a stopping rule to prevent overfitting?**
* A) Gini Impurity
* B) Pearson's Chi-square test
* C) Information Gain
* D) Principal Component Analysis

**79. When interpreting a decision tree visualization generated by `plot_tree`, the 'samples' attribute in a node box indicates:**
* A) The exact prediction accuracy of that specific node.
* B) The total number of training instances that have been routed down the path to reach that specific node.
* C) The minimum number of samples required to split the node further.
* D) The array of feature importance weights.

**80. What is the relationship between 'Information Gain' and 'Entropy'?**
* A) Information Gain is the mathematical reciprocal of Entropy ($1 / Entropy$).
* B) Information Gain is the sum of the Entropies of the child nodes.
* C) Information Gain is the reduction in Entropy achieved by partitioning the data according to a specific attribute.
* D) They are identically synonymous terms with no mathematical distinction.

**81. According to the tutorial, which parameter allows you to evaluate how a model performs on a completely unobserved subset of the original data?**
* A) The `fit()` function.
* B) The test sets generated by `train_test_split` (e.g., `X_test`, `y_test`).
* C) The `max_depth` parameter.
* D) The `LabelEncoder` object.

**82. Why do Decision Trees theoretically require less data preprocessing (like normalization or standardization) compared to algorithms like K-Nearest Neighbors or Support Vector Machines?**
* A) Because decision trees rely on strict distance calculations (like Euclidean distance) which naturally auto-scale.
* B) Because decision trees are built on conditional threshold splits (e.g., value > 50) that are completely invariant to the monotonic scaling or absolute magnitude of the feature values.
* C) Because decision trees inherently scale all data between 0 and 1 internally during the `fit()` method.
* D) Because decision trees only accept binary (0 or 1) input features.

**83. In cross-validation evaluation (like 5-fold CV), how is the dataset utilized?**
* A) It is split into 5 equal parts; the model is trained 5 times, each time using 4 parts for training and a different 1 part for testing.
* B) It is multiplied by a factor of 5 to synthesize new data.
* C) It is split into 5 parts, and only the single best-performing part is used for training.
* D) It enforces a maximum tree depth of 5.

**84. If a decision tree node has 4 samples of Class A and 0 samples of Class B, what is the value of its Entropy?**
* A) 1.0
* B) 0.5
* C) 0.0
* D) undefined

**85. What is the specific computational advantage of using the Gini Index over Entropy?**
* A) Gini Index guarantees a higher accuracy score on the test set.
* B) Gini Index avoids the computationally expensive logarithmic calculation ($\log_2$) required by Entropy, relying instead on simple squaring and summing.
* C) Gini Index can process raw string data without numerical encoding.
* D) Gini Index automatically performs post-pruning without human intervention.

**86. When a Decision Tree is described as an "eager learner", what does this contrast with?**
* A) Algorithms that cannot learn from categorical data.
* B) "Lazy learners" (like K-Nearest Neighbors) that do not build a definitive model during the training phase, but instead wait until a query is made to perform generalization.
* C) Unsupervised learning models.
* D) Algorithms that require hyperparameter tuning.

**87. In an ensemble model like Random Forest, why is it critical that the individual trees be diverse (uncorrelated)?**
* A) Because identical trees would cause the Python execution to deadlock.
* B) Because averaging the predictions of highly correlated (similar) trees does not significantly reduce the overall variance of the ensemble.
* C) Because highly diverse trees use fewer memory resources.
* D) Because diversity forces the Gini index to reach 1.0 faster.

**88. In the formula for Gain Ratio: $GainRatio(A) = \frac{Gain(A)}{SplitInfo(A)}$, what does $SplitInfo(A)$ represent?**
* A) The Gini Impurity of the attribute A.
* B) The total execution time required to split the node.
* C) The intrinsic entropy generated simply by splitting the data into subsets according to the categories of attribute A, heavily penalizing attributes with many small branches.
* D) The specific test set accuracy achieved after splitting on A.

**89. If a dataset is preprocessed using `pd.factorize()` instead of `LabelEncoder`, what is the primary structural difference in the output?**
* A) `factorize()` returns a tuple containing the integer-encoded array and an Index object of the original unique categories, whereas `LabelEncoder` requires an explicit object instantiation to transform and inverse-transform.
* B) `factorize()` automatically applies One-Hot Encoding.
* C) `factorize()` normalizes numerical values between -1 and 1.
* D) There is no difference; they are exactly identical aliases in the same library.

**90. Which combination of precision and recall indicates a model that flags almost everything as positive (high rate of false positives), but rarely misses an actual positive case?**
* A) High Precision, High Recall
* B) Low Precision, High Recall
* C) High Precision, Low Recall
* D) Low Precision, Low Recall

**91. What is the interpretation of "Precision" in a binary classification matrix?**
* A) Of all the actual positive instances in the dataset, what fraction did the model correctly identify?
* B) Of all the instances the model *predicted* as positive, what fraction were *actually* positive?
* C) The harmonic mean of True Positives and False Negatives.
* D) The absolute ratio of True Positives to True Negatives.

**92. If `$min_samples_leaf=5$` is applied to a Decision Tree, what is mathematically prevented from occurring?**
* A) The tree will not be allowed to exceed a depth of 5.
* B) Any proposed split that results in a child (leaf) node containing fewer than 5 training instances will be rejected, and the parent node will not be split.
* C) The tree will only evaluate the top 5 most important features.
* D) The tree will randomly drop 5 samples from the dataset to ensure bootstrapping.

**93. Which of the following best describes the "Bootstrap" component of Bootstrap Aggregating (Bagging)?**
* A) Resampling the training dataset *with replacement* to create multiple different training subsets of the same size.
* B) Booting up multiple server instances to process the decision tree in parallel.
* C) Iteratively increasing the depth of the tree from 1 to N until error converges.
* D) Extracting the residual errors and strapping them to the input of the next tree.

**94. A Decision Tree with highly localized, fragmented, and extremely complex decision boundaries on a 2D scatter plot is displaying classic signs of:**
* A) Severe Underfitting
* B) Appropriate generalization
* C) Severe Overfitting
* D) High Bias

**95. How does the `RandomForestClassifier` determine "Feature Importance" internally?**
* A) By asking a human operator to rank the features prior to training.
* B) By evaluating how much each feature decreases the overall impurity (e.g., Gini or Entropy) across all the nodes and trees where it is used to split.
* C) By randomly assigning a weight to each feature and adjusting it via gradient descent.
* D) By measuring the correlation of each feature to the index column.

**96. In the tutorial, the metric `accuracy_score(y_test, y_pred)` is used. What are `y_test` and `y_pred` respectively?**
* A) The training features and the testing features.
* B) The ground-truth (actual) labels of the unseen test set, and the labels predicted by the model for that exact same test set.
* C) The input features and the probability outputs.
* D) The baseline model and the optimized model.

**97. If you print a trained Scikit-Learn decision tree's `.tree_.max_depth` attribute and it reads 15, while your specified `max_depth` parameter was `None`, what occurred?**
* A) The algorithm crashed at depth 15 due to memory constraints.
* B) The tree naturally halted its growth at depth 15 because all resulting leaf nodes became pure or met other stopping criteria (like `min_samples_split`).
* C) Scikit-Learn has a hard-coded maximum limit of 15 for all decision trees.
* D) The algorithm randomly selected 15 features to split on.

**98. The specific sequence of splitting decisions from the Root node to any specific Leaf node represents:**
* A) A random baseline assumption.
* B) A logical boolean rule (e.g., IF Outlook=Sunny AND Humidity=Normal THEN Play=Yes).
* C) The calculation of the Gini impurity.
* D) A continuous regression line.

**99. Which of the following is true concerning the handling of missing values (NaN) within the standard Scikit-Learn `DecisionTreeClassifier` (as of standard implementations discussed in basic tutorials)?**
* A) It inherently supports NaN values by sending them down both branches with weighted probabilities.
* B) It automatically imputes NaN values with the mean of the column during the `fit()` process.
* C) It does not directly support missing values; they must be dropped (e.g., `dropna()`) or imputed prior to training, otherwise an error is raised.
* D) It maps all missing values to a new categorical label called 'Unknown'.

**100. A Decision Tree model evaluates an attribute "Income" which ranges from \$10,000 to \$150,000. It determines the optimal split is $Income \le 55000$. This specific decision rule is generated at what type of node?**
* A) A Leaf Node
* B) A Pruned Node
* C) An Internal (Decision) Node
* D) A Forest Node

---

### **Answers: Questions 1 – 50**

| | | | | | | | | | |
|---|---|---|---|---|---|---|---|---|---|
| **1.** C | **2.** B | **3.** D | **4.** B | **5.** C | **6.** B | **7.** B | **8.** C | **9.** C | **10.** B |
| **11.** D | **12.** C | **13.** C | **14.** B | **15.** C | **16.** B | **17.** C | **18.** C | **19.** C | **20.** B |
| **21.** C | **22.** B | **23.** C | **24.** D | **25.** C | **26.** B | **27.** B | **28.** A | **29.** B | **30.** B |
| **31.** C | **32.** B | **33.** B | **34.** A | **35.** A | **36.** B | **37.** B | **38.** C | **39.** A | **40.** B |
| **41.** B | **42.** D | **43.** B | **44.** C | **45.** B | **46.** C | **47.** C | **48.** B | **49.** C | **50.** B |

<br>

### **Answers: Questions 51 – 100**

| | | | | | | | | | |
|---|---|---|---|---|---|---|---|---|---|
| **51.** B | **52.** B | **53.** C | **54.** C | **55.** B | **56.** B | **57.** C | **58.** B | **59.** C | **60.** C |
| **61.** A | **62.** B | **63.** C | **64.** B | **65.** B | **66.** B | **67.** C | **68.** B | **69.** B | **70.** B |
| **71.** A | **72.** B | **73.** C | **74.** B | **75.** C | **76.** B | **77.** C | **78.** B | **79.** B | **80.** C |
| **81.** B | **82.** B | **83.** A | **84.** C | **85.** B | **86.** B | **87.** B | **88.** C | **89.** A | **90.** B |
| **91.** B | **92.** B | **93.** A | **94.** C | **95.** B | **96.** B | **97.** B | **98.** B | **99.** C | **100.** C |